# Быстрый старт: Тестирование модулей OLX и Satu

**Дата создания:** 18.11.2025 21:11


**Статус**: Базовая авторизация готова к тестированию  
**Дата**: 18 ноября 2025

---

## 📋 Что создано

✅ **Полная архитектура** двух автономных модулей (OLX и Satu)  
✅ **База данных** - 10 таблиц в Supabase (4 для OLX + 6 для Satu)  
✅ **Авторизация** - OAuth 2.0 для OLX, API токены для Satu  
✅ **FastAPI приложения** - порт 8001 (OLX) и 8002 (Satu)  
✅ **Pydantic модели** - 45+ моделей с валидацией  
✅ **Документация** - README и API docs для каждого модуля

---

## 🚀 Быстрое тестирование

### Шаг 1: Применить SQL схемы

```bash
# В Supabase SQL Editor выполнить:
# 1. modules/platforms/olx/database/schema.sql
# 2. modules/platforms/satu/database/schema.sql
```

### Шаг 2: Установить зависимости

```bash
cd /Users/vbut/lead-generation-system

# Для OLX модуля
cd modules/platforms/olx
pip install -r requirements.txt

# Для Satu модуля
cd ../satu
pip install -r requirements.txt
```

### Шаг 3: Настроить окружение

Создайте `.env` файл в корне проекта или в каждом модуле:

```bash
# OLX настройки
OLX_SUPABASE_URL=https://your-project.supabase.co
OLX_SUPABASE_KEY=your-anon-key
OLX_CLIENT_ID=your_olx_client_id
OLX_CLIENT_SECRET=your_olx_client_secret

# Satu настройки
SATU_SUPABASE_URL=https://your-project.supabase.co
SATU_SUPABASE_KEY=your-anon-key
```

### Шаг 4: Запустить модули

```bash
# Терминал 1 - OLX модуль
cd modules/platforms/olx
python -m api.main
# Доступно на http://localhost:8001/docs

# Терминал 2 - Satu модуль
cd modules/platforms/satu
python -m api.main
# Доступно на http://localhost:8002/docs
```

### Шаг 5: Протестировать API

#### OLX OAuth авторизация:
1. Открыть http://localhost:8001/docs
2. Перейти к `POST /auth/oauth/login`
3. Ввести credentials:
   ```json
   {
     "email": "your@email.com",
     "password": "your_password",
     "client_id": "your_client_id",
     "client_secret": "your_client_secret"
   }
   ```
4. Получить токен и account info

#### Satu API Token:
1. Открыть http://localhost:8002/docs
2. Перейти к `POST /auth/token`
3. Ввести:
   ```json
   {
     "company_name": "Ваша компания",
     "api_token": "your_satu_api_token"
   }
   ```
4. Получить account info

---

## 📊 Проверить результаты

После успешной авторизации проверьте в Supabase:

```sql
-- OLX аккаунты
SELECT * FROM olx_accounts;

-- Satu аккаунты
SELECT * FROM satu_accounts;
```

---

## ⚠️ Известные ограничения

1. **OLX OAuth** - требует регистрацию на developer.olx.kz
2. **Satu API Token** - требует компанию на Satu.kz с активным API
3. **Playwright** - пока не реализован (только OAuth/API)
4. **Парсинг** - пока не реализован
5. **Публикация** - пока не реализована

---

## 🛠️ Что делать дальше

### Если авторизация работает:
✅ Переходить к реализации парсеров  
✅ Тестировать публикацию объявлений  
✅ Создать SDK для интеграции

### Если есть ошибки:
1. Проверьте SQL схемы в Supabase
2. Проверьте .env файл
3. Проверьте логи модулей
4. Проверьте Supabase credentials

---

## 📝 Дополнительные команды

```bash
# Проверить health endpoints
curl http://localhost:8001/health
curl http://localhost:8002/health

# Список аккаунтов
curl http://localhost:8001/auth/accounts
curl http://localhost:8002/auth/accounts

# Информация об аккаунте
curl http://localhost:8001/auth/accounts/{account_id}
curl http://localhost:8002/auth/accounts/{account_id}
```

---

## 📖 Полная документация

- [Детальный отчет о прогрессе](/docs/PLATFORM_MODULES_PROGRESS_REPORT.md)
- [OLX Module README](/modules/platforms/olx/README.md)
- [Satu Module README](/modules/platforms/satu/README.md)
- [OLX API Docs](http://localhost:8001/docs) (после запуска)
- [Satu API Docs](http://localhost:8002/docs) (после запуска)

---

**Вопросы?** Проверьте логи модулей или отчет о прогрессе.


