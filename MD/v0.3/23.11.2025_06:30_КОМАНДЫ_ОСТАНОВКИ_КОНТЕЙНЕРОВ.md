# 🛑 Команды для остановки всех контейнеров

**Дата создания:** 23.11.2025 06:30  
**Статус:** 📝 Инструкция

---

## ⚠️ Проблема с SSH

SSH подключение не работает (таймаут). Выполни команды вручную через терминал.

---

## 🔧 Команды для остановки

### Подключись к серверу:

```bash
ssh -i ~/.ssh/new-server-key.pem ec2-user@35.158.118.225
```

### Останови все контейнеры:

```bash
# Вариант 1: Через docker-compose (рекомендуется)
cd /home/ec2-user/lead-generation-system
docker-compose down --remove-orphans

# Вариант 2: Остановить все запущенные контейнеры
docker stop $(docker ps -q)

# Вариант 3: Убить все процессы (принудительно)
docker kill $(docker ps -q)
```

### Проверь, что всё остановлено:

```bash
docker ps
# Должно быть пусто
```

---

## 🔄 Если нужно перезапустить

```bash
cd /home/ec2-user/lead-generation-system
docker-compose up -d --build
```

---

**Дата создания:** 23.11.2025 06:30  
**Статус:** 📝 **Выполни команды вручную**

