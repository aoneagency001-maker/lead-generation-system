# Telegram Assistant Bot с Gemini API

**Дата создания:** 20.11.2025 04:52

## 🎯 Описание

Telegram бот-ассистент на базе **Google Gemini API**, который поддерживает:
- 💬 **Текстовые сообщения** - полноценный диалог с контекстом
- 🎤 **Голосовые сообщения** - через распознавание речи Telegram
- 🖼️ **Изображения** - анализ через Gemini Vision API
- 📝 **История разговора** - бот помнит контекст диалога

---

## 🏗️ Архитектура

### Компоненты

1. **TelegramAssistantBot** (`shared/telegram_assistant_bot.py`)
   - Основной класс бота
   - Обработка обновлений от Telegram
   - Интеграция с Gemini API

2. **Gemini API Integration**
   - `gemini-pro` - для текстовых сообщений
   - `gemini-pro-vision` - для анализа изображений

3. **База данных**
   - Таблица `telegram_bot_subscribers` - подписчики
   - Автоматическое сохранение при `/start`

---

## 📦 Установка

### 1. Зависимости

```bash
pip install google-generativeai Pillow
```

Или обновить все:
```bash
pip install -r requirements.txt
```

### 2. Переменные окружения

Добавь в `.env`:

```bash
# Telegram Assistant Bot
TELEGRAM_ASSISTANT_BOT_TOKEN=твой_токен_бота
GEMINI_API_KEY=твой_gemini_ключ

# Опционально
GEMINI_MODEL=gemini-pro
GEMINI_VISION_MODEL=gemini-pro-vision
```

### 3. Получение ключей

**Telegram Bot Token:**
1. Открой [@BotFather](https://t.me/BotFather)
2. `/newbot` → следуй инструкциям
3. Скопируй токен

**Gemini API Key:**
1. Зайди на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Создай API ключ
3. Скопируй ключ

---

## 🚀 Запуск

### Вариант 1: Через скрипт

```bash
./scripts/start_assistant_bot.sh
```

### Вариант 2: Напрямую

```bash
python3 -m shared.telegram_assistant_bot
```

---

## 💬 Использование

### Команды

- `/start` - Начать работу с ботом
- `/help` - Справка по командам
- `/clear` - Очистить историю разговора

### Примеры

**Текстовое сообщение:**
```
Пользователь: "Расскажи о Python"
Бот: [Ответ от Gemini API]
```

**Изображение:**
```
Пользователь: [Отправляет фото]
Бот: "🖼️ Анализирую изображение..."
Бот: [Описание изображения от Gemini Vision]
```

**Голосовое сообщение:**
```
Пользователь: [Отправляет голосовое]
Бот: "🎤 Обрабатываю голосовое сообщение..."
Бот: "📝 Распознанный текст: [текст]"
Бот: [Ответ от Gemini API]
```

---

## 🔧 Технические детали

### Обработка текста

```python
# История разговора хранится в памяти
conversation_history[chat_id] = [
    {"role": "user", "parts": ["Привет"]},
    {"role": "model", "parts": ["Здравствуй!"]},
    ...
]

# Отправка в Gemini
response = gemini_model.generate_content(history)
```

### Обработка изображений

```python
# Скачивание изображения
image_data = await download_file(file_path)

# Отправка в Gemini Vision
response = gemini_vision_model.generate_content([prompt, image])
```

### Обработка голоса

1. Проверяем, есть ли распознанный текст от Telegram
2. Если есть - обрабатываем как текст
3. Если нет - предлагаем использовать текст

---

## 🗄️ Интеграция с базой данных

### Автоматическое сохранение подписчиков

При команде `/start` автоматически сохраняется:

```python
{
    "bot_type": "assistant",
    "chat_id": 123456789,
    "username": "user",
    "first_name": "Имя",
    "last_name": "Фамилия",
    "language_code": "ru",
    "status": "active"
}
```

### Обновление активности

При каждом сообщении обновляется `last_activity_at`.

### Запросы к БД

```sql
-- Все подписчики assistant бота
SELECT * FROM telegram_bot_subscribers 
WHERE bot_type = 'assistant';

-- Активные подписчики
SELECT * FROM telegram_bot_subscribers 
WHERE bot_type = 'assistant' AND status = 'active';

-- Статистика
SELECT 
    COUNT(*) as total,
    COUNT(*) FILTER (WHERE status = 'active') as active
FROM telegram_bot_subscribers
WHERE bot_type = 'assistant';
```

---

## 🐛 Решение проблем

### Ошибка: "TELEGRAM_ASSISTANT_BOT_TOKEN not set"
**Решение:** Проверь `.env` файл, добавь токен

### Ошибка: "GEMINI_API_KEY not set"
**Решение:** Добавь Gemini API ключ в `.env`

### Ошибка: "google-generativeai not installed"
**Решение:** 
```bash
pip install google-generativeai
```

### Бот не отвечает
**Проверь:**
1. Бот запущен?
2. Правильный токен?
3. Интернет работает?
4. Gemini API ключ валидный?

### Изображения не обрабатываются
**Проверь:**
1. Установлен Pillow? (`pip install Pillow`)
2. Gemini Vision модель доступна?
3. Размер изображения не слишком большой?

---

## 📊 Мониторинг

### Логи

Бот логирует:
- Инициализацию
- Ошибки обработки
- Сохранение подписчиков
- Обработку сообщений

### Метрики

Можно отслеживать:
- Количество подписчиков
- Активность пользователей
- Типы сообщений (текст/голос/изображение)
- Ошибки обработки

---

## 🔐 Безопасность

1. **Токены в .env** - никогда не коммить в git
2. **API ключи** - хранить секретно
3. **Лимиты Gemini** - следить за квотами API
4. **История разговоров** - хранится в памяти, можно перенести в БД

---

## 🚀 Улучшения в будущем

- [ ] Сохранение истории разговоров в БД
- [ ] Поддержка файлов (PDF, документы)
- [ ] Мультиязычность
- [ ] Настройки пользователя (язык, модель)
- [ ] Статистика использования
- [ ] Интеграция с другими AI моделями

---

## 📚 Ссылки

- **Код бота:** `shared/telegram_assistant_bot.py`
- **Конфигурация:** `core/api/config.py`
- **Быстрый старт:** `TELEGRAM_ASSISTANT_BOT_QUICKSTART.md`
- **Gemini API Docs:** https://ai.google.dev/docs

---

**Готово! Бот готов к использованию! 🎉**

