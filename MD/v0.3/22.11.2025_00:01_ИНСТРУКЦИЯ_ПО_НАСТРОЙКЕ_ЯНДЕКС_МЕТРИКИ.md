# Инструкция по настройке Яндекс.Метрики для автономной работы

**Дата создания:** 22.11.2025 00:01

## 📋 Содержание

1. [Обзор](#обзор)
2. [Необходимые данные](#необходимые-данные)
3. [Получение OAuth токена](#получение-oauth-токена)
4. [Получение ID счетчика](#получение-id-счетчика)
5. [Доступные API](#доступные-api)
6. [Настройка для автономной работы](#настройка-для-автономной-работы)
7. [Примеры использования](#примеры-использования)
8. [Безопасность](#безопасность)

---

## 🎯 Обзор

Яндекс.Метрика предоставляет несколько API для работы с данными:

1. **Management API** - управление счетчиками, целями, фильтрами
2. **Logs API** - экспорт сырых данных (визиты, хиты)
3. **Reporting API** - готовые отчеты и метрики
4. **Real-time API** - данные в реальном времени

Для автономной работы системы нам понадобятся:
- **OAuth токен** - для авторизации
- **ID счетчика** - идентификатор вашего счетчика Метрики
- **Настройки доступа** - права доступа к данным

---

## 🔑 Необходимые данные

### 1. OAuth токен Яндекс.Метрики

**Что это:** Токен доступа для работы с API Яндекс.Метрики

**Формат:** Строка вида `AQAAAAA...` (обычно 40+ символов)

**Где хранить:** В переменных окружения `.env` как `YANDEX_METRIKA_TOKEN`

**Срок действия:** 
- Временный токен: 1 год (можно продлевать)
- Постоянный токен: бессрочный (требует настройки приложения)

### 2. ID счетчика Яндекс.Метрики

**Что это:** Числовой идентификатор вашего счетчика Метрики

**Формат:** Число (например: `12345678`)

**Где хранить:** В переменных окружения `.env` как `YANDEX_METRIKA_COUNTER_ID`

**Как найти:** В интерфейсе Метрики → Настройки → Код счетчика

### 3. Права доступа (Scope)

**Что это:** Разрешения, которые дает токен

**Необходимые права:**
- `metrika:read` - чтение данных
- `metrika:write` - запись данных (для целей, фильтров)

**Где указывать:** При создании OAuth приложения

---

## 🔐 Получение OAuth токена

### Способ 1: Через Яндекс OAuth (Рекомендуется)

**Шаг 1: Создание OAuth приложения**

1. Перейдите на [https://oauth.yandex.ru/](https://oauth.yandex.ru/)
2. Войдите в свой Яндекс аккаунт
3. Нажмите **"Зарегистрировать новое приложение"**
4. Заполните форму:
   - **Название:** `Lead Generation System` (или любое другое)
   - **Платформы:** Выберите "Веб-сервисы"
   - **Callback URI:** `https://oauth.yandex.ru/verification_code` (для получения кода)
   - **Права доступа:** 
     - ✅ `Доступ к данным Яндекс.Метрики (metrika:read)`
     - ✅ `Управление данными Яндекс.Метрики (metrika:write)` (опционально)
5. Нажмите **"Создать приложение"**

**Шаг 2: Получение Client ID и Client Secret**

После создания приложения вы получите:
- **Client ID** - публичный идентификатор (например: `abc123def456`)
- **Client Secret** - секретный ключ (например: `xyz789uvw012`)

**Сохраните эти данные!** Они понадобятся для получения токена.

**Шаг 3: Получение OAuth токена**

**Вариант A: Временный токен (через браузер)**

1. Откройте в браузере:
```
https://oauth.yandex.ru/authorize?response_type=token&client_id=ВАШ_CLIENT_ID
```

2. Разрешите доступ приложению
3. После редиректа в адресной строке будет токен:
```
https://oauth.yandex.ru/verification_code#access_token=AQAAAAA...&token_type=bearer&expires_in=31536000
```

4. Скопируйте значение `access_token` (часть после `access_token=` до `&`)

**Вариант B: Постоянный токен (через API)**

Используйте следующий запрос:

```bash
curl -X POST https://oauth.yandex.ru/token \
  -d "grant_type=authorization_code" \
  -d "code=КОД_ИЗ_ШАГА_1" \
  -d "client_id=ВАШ_CLIENT_ID" \
  -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Вариант C: Refresh Token (для автоматического обновления)**

1. Получите `refresh_token` при первом получении токена
2. Используйте его для автоматического обновления:

```bash
curl -X POST https://oauth.yandex.ru/token \
  -d "grant_type=refresh_token" \
  -d "refresh_token=ВАШ_REFRESH_TOKEN" \
  -d "client_id=ВАШ_CLIENT_ID" \
  -d "client_secret=ВАШ_CLIENT_SECRET"
```

### Способ 2: Через Яндекс ID (Упрощенный)

1. Перейдите на [https://yandex.ru/dev/id/](https://yandex.ru/dev/id/)
2. Создайте приложение
3. Получите токен аналогично Способу 1

---

## 🆔 Получение ID счетчика

### Способ 1: Через интерфейс Метрики

1. Войдите в [Яндекс.Метрику](https://metrika.yandex.ru/)
2. Выберите нужный счетчик
3. Перейдите в **Настройки** → **Код счетчика**
4. ID счетчика указан в коде:
```html
<!-- Яндекс.Метрика -->
<script type="text/javascript">
   (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   ...
   yaCounter12345678  <!-- ← Это ID счетчика -->
```

### Способ 2: Через Management API

Если у вас уже есть токен, можно получить список счетчиков:

```bash
curl -H "Authorization: OAuth ВАШ_ТОКЕН" \
  https://api-metrika.yandex.net/management/v1/counters
```

Ответ будет содержать список всех счетчиков с их ID.

### Способ 3: Из URL

ID счетчика виден в URL при открытии счетчика:
```
https://metrika.yandex.ru/dashboard?id=12345678
                                    ^^^^^^^^
                                    Это ID
```

---

## 📡 Доступные API

### 1. Management API

**Назначение:** Управление счетчиками, целями, фильтрами

**Базовый URL:** `https://api-metrika.yandex.net/management/v1/`

**Основные endpoints:**
- `GET /counters` - список счетчиков
- `GET /counter/{counterId}` - информация о счетчике
- `GET /counter/{counterId}/goals` - список целей
- `POST /counter/{counterId}/goals` - создание цели

**Пример запроса:**
```bash
curl -H "Authorization: OAuth ВАШ_ТОКЕН" \
  https://api-metrika.yandex.net/management/v1/counters
```

### 2. Logs API

**Назначение:** Экспорт сырых данных (визиты, хиты)

**Базовый URL:** `https://api-metrika.yandex.net/logs/v1/export/`

**Основные endpoints:**
- `POST /visits` - создание запроса на экспорт визитов
- `GET /{logRequestId}/status` - проверка статуса обработки
- `GET /{logRequestId}/part/{partNumber}/download` - скачивание части данных

**Пример использования:**
```bash
# 1. Создать запрос на экспорт
curl -X POST \
  -H "Authorization: OAuth ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{
    "date1": "2025-11-01",
    "date2": "2025-11-22",
    "fields": ["ym:s:visitID", "ym:s:dateTime", "ym:s:pageURL"]
  }' \
  "https://api-metrika.yandex.net/logs/v1/export/visits?counter_id=12345678"

# 2. Проверить статус (получите log_request_id из шага 1)
curl -H "Authorization: OAuth ВАШ_ТОКЕН" \
  "https://api-metrika.yandex.net/logs/v1/export/12345/status?counter_id=12345678"

# 3. Скачать данные (когда статус = "processed")
curl -H "Authorization: OAuth ВАШ_ТОКЕН" \
  "https://api-metrika.yandex.net/logs/v1/export/12345/part/0/download?counter_id=12345678"
```

### 3. Reporting API

**Назначение:** Готовые отчеты и метрики

**Базовый URL:** `https://api-metrika.yandex.net/stat/v1/data/`

**Основные endpoints:**
- `GET /data` - получение данных отчета

**Пример запроса:**
```bash
curl -H "Authorization: OAuth ВАШ_ТОКЕН" \
  "https://api-metrika.yandex.net/stat/v1/data?ids=12345678&metrics=ym:s:visits&date1=2025-11-01&date2=2025-11-22"
```

### 4. Real-time API

**Назначение:** Данные в реальном времени

**Базовый URL:** `https://api-metrika.yandex.net/stat/v1/data/`

**Пример запроса:**
```bash
curl -H "Authorization: OAuth ВАШ_ТОКЕН" \
  "https://api-metrika.yandex.net/stat/v1/data?ids=12345678&metrics=ym:s:visits&realtime=true"
```

---

## ⚙️ Настройка для автономной работы

### 1. Добавление переменных в `.env`

Создайте или обновите файл `.env` в корне проекта:

```bash
# Яндекс.Метрика
YANDEX_METRIKA_TOKEN=AQAAAAA...ваш_токен...
YANDEX_METRIKA_COUNTER_ID=12345678

# Опционально: для автоматического обновления токена
YANDEX_METRIKA_CLIENT_ID=ваш_client_id
YANDEX_METRIKA_CLIENT_SECRET=ваш_client_secret
YANDEX_METRIKA_REFRESH_TOKEN=ваш_refresh_token
```

### 2. Структура данных для хранения

Для автономной работы система должна хранить:

```python
# Пример структуры конфигурации
METRIKA_CONFIG = {
    "token": "AQAAAAA...",
    "counter_id": "12345678",
    "client_id": "abc123...",  # опционально
    "client_secret": "xyz789...",  # опционально
    "refresh_token": "def456...",  # опционально
    "token_expires_at": "2026-11-22T00:00:00Z",  # если известен срок действия
}
```

### 3. Автоматическое обновление токена

Если токен имеет срок действия, настройте автоматическое обновление:

```python
# Псевдокод для обновления токена
async def refresh_metrika_token():
    if token_expires_soon():
        new_token = await get_new_token_using_refresh_token()
        update_env_file(new_token)
        reload_config()
```

---

## 💻 Примеры использования

### Пример 1: Получение списка счетчиков

```python
import httpx
import os

async def get_metrika_counters():
    token = os.getenv("YANDEX_METRIKA_TOKEN")
    
    async with httpx.AsyncClient() as client:
        response = await client.get(
            "https://api-metrika.yandex.net/management/v1/counters",
            headers={"Authorization": f"OAuth {token}"}
        )
        return response.json()

# Использование
counters = await get_metrika_counters()
print(f"Найдено счетчиков: {len(counters['counters'])}")
```

### Пример 2: Экспорт данных визитов

```python
import httpx
import os
import asyncio

async def export_metrika_visits(date_from: str, date_to: str):
    token = os.getenv("YANDEX_METRIKA_TOKEN")
    counter_id = os.getenv("YANDEX_METRIKA_COUNTER_ID")
    
    async with httpx.AsyncClient() as client:
        # 1. Создать запрос на экспорт
        response = await client.post(
            f"https://api-metrika.yandex.net/logs/v1/export/visits",
            params={"counter_id": counter_id},
            headers={
                "Authorization": f"OAuth {token}",
                "Content-Type": "application/json"
            },
            json={
                "date1": date_from,
                "date2": date_to,
                "fields": [
                    "ym:s:visitID",
                    "ym:s:dateTime",
                    "ym:s:pageURL",
                    "ym:s:UTMSource",
                    "ym:s:UTMMedium",
                    "ym:s:UTMCampaign",
                ]
            }
        )
        
        log_request = response.json()
        log_request_id = log_request["log_request_id"]
        
        # 2. Ждем обработки (проверяем статус каждые 5 секунд)
        while True:
            status_response = await client.get(
                f"https://api-metrika.yandex.net/logs/v1/export/{log_request_id}/status",
                params={"counter_id": counter_id},
                headers={"Authorization": f"OAuth {token}"}
            )
            status = status_response.json()["status"]
            
            if status == "processed":
                break
            elif status == "canceled":
                raise Exception("Запрос отменен")
            
            await asyncio.sleep(5)
        
        # 3. Скачиваем данные
        parts = log_request["parts"]
        all_data = []
        
        for part_num in range(parts):
            download_response = await client.get(
                f"https://api-metrika.yandex.net/logs/v1/export/{log_request_id}/part/{part_num}/download",
                params={"counter_id": counter_id},
                headers={"Authorization": f"OAuth {token}"}
            )
            
            # Данные в формате TSV
            lines = download_response.text.strip().split("\n")
            for line in lines:
                if line:
                    all_data.append(line.split("\t"))
        
        return all_data

# Использование
visits = await export_metrika_visits("2025-11-01", "2025-11-22")
print(f"Экспортировано визитов: {len(visits)}")
```

### Пример 3: Получение готовых отчетов

```python
import httpx
import os

async def get_metrika_report(metrics: list[str], date_from: str, date_to: str):
    token = os.getenv("YANDEX_METRIKA_TOKEN")
    counter_id = os.getenv("YANDEX_METRIKA_COUNTER_ID")
    
    async with httpx.AsyncClient() as client:
        response = await client.get(
            "https://api-metrika.yandex.net/stat/v1/data",
            params={
                "ids": counter_id,
                "metrics": ",".join(metrics),
                "date1": date_from,
                "date2": date_to,
            },
            headers={"Authorization": f"OAuth {token}"}
        )
        return response.json()

# Использование
report = await get_metrika_report(
    metrics=["ym:s:visits", "ym:s:pageviews", "ym:s:users"],
    date_from="2025-11-01",
    date_to="2025-11-22"
)
print(f"Визитов: {report['data'][0]['metrics'][0]}")
```

---

## 🔒 Безопасность

### 1. Защита токена

**❌ НЕ ДЕЛАЙТЕ:**
- Не коммитьте `.env` в Git
- Не передавайте токен в URL
- Не логируйте токен в открытом виде

**✅ ДЕЛАЙТЕ:**
- Храните токен в `.env` (добавьте `.env` в `.gitignore`)
- Используйте переменные окружения
- Шифруйте токен при хранении в базе данных
- Используйте HTTPS для всех запросов

### 2. Ограничение прав доступа

При создании OAuth приложения указывайте только необходимые права:
- Для чтения данных: `metrika:read`
- Для записи данных: `metrika:write` (только если нужно)

### 3. Ротация токенов

Если токен скомпрометирован:
1. Отзовите токен в настройках приложения
2. Создайте новый токен
3. Обновите `.env`

### 4. Мониторинг использования

Следите за:
- Количеством запросов к API (лимиты: ~10,000 запросов/день)
- Необычной активностью
- Ошибками авторизации

---

## 📊 Полезные метрики для Market Research

Для модуля Market Research могут быть полезны следующие метрики:

### Посещаемость
- `ym:s:visits` - количество визитов
- `ym:s:users` - количество уникальных посетителей
- `ym:s:pageviews` - количество просмотров страниц

### Источники трафика
- `ym:s:UTMSource` - источник (utm_source)
- `ym:s:UTMMedium` - канал (utm_medium)
- `ym:s:UTMCampaign` - кампания (utm_campaign)
- `ym:s:referer` - реферер

### География
- `ym:s:regionCountry` - страна
- `ym:s:regionCity` - город

### Поведение
- `ym:s:bounceRate` - показатель отказов
- `ym:s:avgVisitDurationSeconds` - средняя длительность визита
- `ym:s:pageDepth` - глубина просмотра

### Конверсии
- `ym:s:goal<ID>visits` - достижения цели (требует настройки целей)

---

## 🚀 Следующие шаги

После получения всех необходимых данных:

1. ✅ Добавьте токен и ID счетчика в `.env`
2. ✅ Протестируйте подключение (см. примеры выше)
3. ✅ Настройте автоматический сбор данных
4. ✅ Интегрируйте с модулем Market Research
5. ✅ Настройте мониторинг и алерты

---

## 📚 Дополнительные ресурсы

- [Официальная документация Яндекс.Метрики API](https://yandex.ru/dev/metrika/)
- [Management API](https://yandex.ru/dev/metrika/doc/api2/management/intro.html)
- [Logs API](https://yandex.ru/dev/metrika/doc/api2/logs/about.html)
- [Reporting API](https://yandex.ru/dev/metrika/doc/api2/api_v1/intro.html)
- [OAuth Яндекс](https://yandex.ru/dev/id/doc/ru/)

---

## ❓ Часто задаваемые вопросы

**Q: Токен истекает через год. Что делать?**
A: Используйте `refresh_token` для автоматического обновления или создайте новое приложение с постоянным токеном.

**Q: Сколько запросов можно делать в день?**
A: Обычно лимит составляет 10,000 запросов в день. При превышении лимита запросы будут отклоняться.

**Q: Можно ли использовать один токен для нескольких счетчиков?**
A: Да, если у токена есть доступ ко всем счетчикам. Укажите нужный `counter_id` в каждом запросе.

**Q: Как получить данные за большой период?**
A: Используйте Logs API с разбивкой по месяцам или неделям, так как есть ограничения на размер экспорта.

**Q: Что делать, если токен не работает?**
A: Проверьте:
1. Правильность токена (нет лишних пробелов)
2. Срок действия токена
3. Права доступа приложения
4. Правильность формата заголовка `Authorization: OAuth {token}`

---

**Готово к использованию!** 🎉

После получения всех данных система сможет работать с Яндекс.Метрикой автономно.

