# ⚡ Быстрая настройка автоматического деплоя

**Дата создания:** 18.11.2025 00:42


## 🎯 Что было создано

✅ **GitHub Actions workflow** - автоматический деплой каждые 4 часа  
✅ **Скрипт резервного копирования** - локальные бэкапы перед каждым деплоем  
✅ **Скрипт ручного деплоя** - деплой по требованию  
✅ **Production Docker конфигурация** - готово к продакшену  
✅ **Полная документация** - `DEPLOYMENT.md`

---

## 🚀 Быстрый старт (3 шага)

### Шаг 1: Настройка GitHub Secrets

```bash
# Запустите помощник
./scripts/setup-github-secrets.sh
```

Или вручную:
1. Откройте: https://github.com/aoneagency001-maker/lead-generation-system/settings/secrets/actions
2. Добавьте Secrets:
   - `VPS_HOST` - IP или домен вашего VPS
   - `VPS_USER` - пользователь SSH (обычно `root`)
   - `VPS_SSH_KEY` - приватный SSH ключ (см. ниже)

### Шаг 2: Генерация SSH ключа

```bash
# Создайте SSH ключ для GitHub Actions
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy

# Скопируйте публичный ключ на VPS
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub root@your-vps-ip

# Скопируйте приватный ключ в GitHub Secret
cat ~/.ssh/github_actions_deploy
# Скопируйте ВСЁ (включая BEGIN/END) в VPS_SSH_KEY secret
```

### Шаг 3: Настройка VPS

```bash
# Подключитесь к VPS
ssh root@your-vps-ip

# Установите Docker (если еще не установлен)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Создайте директории
sudo mkdir -p /opt/lead-generation-system
sudo mkdir -p /opt/backups/leadgen
sudo chown -R $USER:$USER /opt/lead-generation-system
```

---

## ⏰ Расписание деплоя

**Текущее:** каждые 4 часа

**Изменить расписание:**
Отредактируйте `.github/workflows/auto-deploy.yml`:

```yaml
schedule:
  - cron: '0 */6 * * *'  # Каждые 6 часов
  # или
  - cron: '0 3 * * *'     # Каждый день в 3:00
```

---

## 📦 Резервное копирование

**Автоматически:**
- Перед каждым деплоем (GitHub Actions)
- При ручном деплое

**Расположение:** `~/leadgen-backups/`

**Ручной бэкап:**
```bash
./scripts/backup.sh
```

**Восстановление:**
```bash
cd ~/leadgen-backups
tar -xzf latest.tar.gz -C /path/to/restore
```

---

## 🔧 Ручной деплой

```bash
# Деплой на production
./scripts/deploy.sh production

# Деплой на staging
./scripts/deploy.sh staging
```

---

## 📊 Мониторинг

**Просмотр деплоев:**
- GitHub → Actions → `🚀 Auto Deploy to VPS`

**Проверка на VPS:**
```bash
ssh root@your-vps-ip "cd /opt/lead-generation-system && docker-compose logs -f"
```

---

## 📚 Полная документация

См. `DEPLOYMENT.md` для детальной информации:
- Настройка VPS
- Troubleshooting
- Безопасность
- Мониторинг

---

## ✅ Чек-лист

- [ ] GitHub Secrets настроены
- [ ] SSH ключ сгенерирован и добавлен на VPS
- [ ] Docker установлен на VPS
- [ ] Директории созданы на VPS
- [ ] Протестирован ручной деплой
- [ ] Проверена работа автоматического деплоя

---

## 🎉 Готово!

После настройки:
1. Автоматический деплой будет запускаться каждые 4 часа
2. Резервные копии будут создаваться автоматически
3. Каждая версия будет помечена тегом

**Первый деплой:** Запустите вручную через `./scripts/deploy.sh` или дождитесь автоматического (максимум 4 часа)

