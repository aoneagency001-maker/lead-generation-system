# Telegram Error Bot - Setup Guide

**Дата создания:** 19.11.2025 23:45

## 🎯 Что это и зачем?

**Telegram Error Bot** - это система мгновенных уведомлений об ошибках и событиях в твоем приложении прямо в Telegram.

### ✅ Что получаешь:

- **Мгновенные уведомления** при любой ошибке в production
- **Полный контекст**: что, где, когда, кто столкнулся с ошибкой
- **Stack trace** - видишь точную строку кода где упало
- **User context** - понимаешь что делал пользователь
- **Startup/Shutdown alerts** - знаешь когда система запустилась/упала
- **Health monitoring** - автоматическая проверка состояния системы
- **$0/месяц** - полностью бесплатно!

---

## 📱 Шаг 1: Создай Telegram Bot (2 минуты)

### 1.1 Открой @BotFather в Telegram

1. В Telegram найди `@BotFather`
2. Нажми **START**

### 1.2 Создай нового бота

Отправь команду:
```
/newbot
```

### 1.3 Введи название и username

```
Bot Name: Lead Gen System Errors
Username: leadgen_errors_bot
```

### 1.4 Получи TOKEN

BotFather отправит тебе сообщение с токеном:
```
7123456789:AAH3jF9k2lM0pQrS1tUvWxYz...
```

**⚠️ ВАЖНО:** Сохрани этот TOKEN - он понадобится дальше!

---

## 🆔 Шаг 2: Получи свой Chat ID (1 минута)

### 2.1 Напиши своему боту

1. Найди своего бота в Telegram (по username который создал)
2. Нажми **START**
3. Напиши любое сообщение, например: `Hello`

### 2.2 Получи Chat ID

Открой в браузере (замени `<TOKEN>` на свой токен):
```
https://api.telegram.org/bot<TOKEN>/getUpdates
```

**Пример:**
```
https://api.telegram.org/bot7123456789:AAH3jF9k2lM0pQrS1tUvWxYz.../getUpdates
```

### 2.3 Найди Chat ID в ответе

В ответе найди строку:
```json
{
  "update_id": 123456789,
  "message": {
    "message_id": 1,
    "from": {
      "id": 987654321,  ← Это твой Chat ID!
      ...
    }
  }
}
```

**Или короче - найди:**
```json
"chat": {
  "id": 987654321,  ← Это твой Chat ID!
  ...
}
```

**⚠️ ВАЖНО:** Сохрани этот Chat ID!

---

## ⚙️ Шаг 3: Добавь в .env (30 секунд)

### 3.1 Открой файл .env

В корне проекта найди файл `.env` (если нет - создай):

```bash
# В терминале
cd /Users/vbut/lead-generation-system
nano .env
```

### 3.2 Добавь Telegram настройки

Добавь в конец файла:
```bash
# Telegram Error Notifications
TELEGRAM_BOT_TOKEN=7123456789:AAH3jF9k2lM0pQrS1tUvWxYz...
TELEGRAM_CHAT_ID=987654321
```

**⚠️ Замени значения на свои!**

### 3.3 Сохрани файл

В nano: `Ctrl+X` → `Y` → `Enter`

---

## ✅ Шаг 4: Протестируй (1 минута)

### 4.1 Перезапусти сервер

```bash
cd /Users/vbut/lead-generation-system
docker-compose restart backend

# Или если без Docker:
# Ctrl+C (остановить)
# python -m core.api.main (запустить снова)
```

### 4.2 Проверь Telegram

Через 5-10 секунд ты должен получить сообщение в Telegram:

```
✅ System

🚀 Lead Generation System started!
Environment: Development
Version: 0.1.0
```

**✅ Если получил - всё работает!**

**❌ Если не получил:**
- Проверь что TOKEN и CHAT_ID правильные
- Проверь что написал боту /start
- Проверь логи сервера: `docker-compose logs backend`

---

## 🧪 Шаг 5: Протестируй уведомление об ошибке

### 5.1 Создай тестовый скрипт

```bash
cd /Users/vbut/lead-generation-system
nano test_telegram.py
```

### 5.2 Вставь код

```python
import asyncio
from shared.telegram_notifier import telegram_notifier

async def test():
    # Тест успешного сообщения
    await telegram_notifier.send_success(
        "✅ Test notification from Lead Gen System!",
        module="Test"
    )
    
    # Тест ошибки
    try:
        raise ValueError("This is a test error!")
    except Exception as e:
        await telegram_notifier.send_error(
            error=e,
            module="TestModule",
            user_context={"test": "value"},
            severity="ERROR"
        )
    
    print("✅ Check your Telegram!")

if __name__ == "__main__":
    asyncio.run(test())
```

### 5.3 Запусти

```bash
python test_telegram.py
```

### 5.4 Проверь Telegram

Ты должен получить 2 сообщения:
1. **✅ Test notification** - успех
2. **❌ ERROR in Production** - тестовая ошибка со stack trace

---

## 📊 Как это работает?

### Автоматические уведомления

#### 1️⃣ **При запуске системы**
Получишь: `✅ System started`

#### 2️⃣ **При остановке системы**
Получишь: `⚠️ System shutting down`

#### 3️⃣ **При любой необработанной ошибке**
Получишь детальное сообщение:
```
❌ ERROR in Production!

⏰ Time: 2025-11-19 23:45:12
📦 Module: API:/api/leads
❌ Error Type: ConnectionError
💬 Message: Failed to connect to database

👤 User Context:
  • method: POST
  • path: /api/leads
  • client_ip: 192.168.1.1

📍 Stack Trace:
  File "main.py", line 142
  File "service.py", line 89
  ...
```

#### 4️⃣ **При проблемах с Health Check**
Если база данных недоступна:
```
⚠️ HealthCheck

⚠️ Database connection lost!
Error: Connection timeout after 30s
```

### Ручные уведомления в коде

В любом модуле можешь использовать:

```python
from shared.telegram_notifier import notify_success, notify_error, notify_warning

# Успех
await notify_success("Lead created successfully!", module="LeadService")

# Предупреждение
await notify_warning("API rate limit approaching", module="OLXParser")

# Ошибка
try:
    result = await risky_operation()
except Exception as e:
    await notify_error(e, module="MyService")
    raise
```

---

## 🎨 Примеры использования

### Пример 1: В сервисе

```python
# modules/whatsapp/service.py
from shared.telegram_notifier import telegram_notifier

class WhatsAppService:
    async def send_message(self, phone: str, text: str):
        try:
            response = await whatsapp_api.send(phone, text)
            
            # Опционально: уведомить об успехе
            await telegram_notifier.send_success(
                f"Message sent to {phone}",
                module="WhatsAppService"
            )
            
            return response
            
        except Exception as e:
            # Автоматически уведомить об ошибке
            await telegram_notifier.send_error(
                error=e,
                module="WhatsAppService.send_message",
                user_context={"phone": phone},
                severity="CRITICAL"
            )
            raise
```

### Пример 2: Мониторинг долгих операций

```python
# modules/olx/parser.py
import time
from shared.telegram_notifier import notify_success

class OLXParser:
    async def parse_ads(self, category: str):
        start = time.time()
        
        try:
            ads = await self._parse(category)
            elapsed = time.time() - start
            
            # Отчет об успехе
            await notify_success(
                f"✅ Parsing completed!\n"
                f"Category: {category}\n"
                f"Ads: {len(ads)}\n"
                f"Time: {elapsed:.1f}s",
                module="OLXParser"
            )
            
            return ads
        except Exception as e:
            await telegram_notifier.send_error(
                error=e,
                module="OLXParser",
                extra_info={"category": category}
            )
            raise
```

### Пример 3: Критические события

```python
from shared.telegram_notifier import telegram_notifier

async def check_disk_space():
    disk_usage = get_disk_usage()
    
    if disk_usage > 90:
        # КРИТИЧНО!
        await telegram_notifier.send_critical(
            message="Disk space critical!",
            module="SystemMonitor",
            details={
                "disk_usage": f"{disk_usage}%",
                "action": "Clean up or expand disk ASAP"
            }
        )
```

---

## 🚨 Уровни важности

### ERROR (❌)
Обычные ошибки - получаешь уведомление
```python
await telegram_notifier.send_error(
    error=exception,
    module="ModuleName",
    severity="ERROR"
)
```

### WARNING (⚠️)
Предупреждения - что-то не так, но не критично
```python
await telegram_notifier.send_warning(
    "API rate limit approaching",
    module="Parser"
)
```

### CRITICAL (🚨)
Критические проблемы - требуют немедленного внимания
```python
await telegram_notifier.send_critical(
    message="Database offline!",
    module="System",
    details={"downtime": "5 minutes"}
)
```

### SUCCESS (✅)
Успешные операции
```python
await telegram_notifier.send_success(
    "Campaign launched successfully",
    module="CampaignService"
)
```

---

## 🛡️ Защита от спама

Система автоматически защищает от спама:

- **Дубликаты ошибок**: одинаковые ошибки не отправляются чаще раза в 5 минут
- **Health check alerts**: проблемы с базой не отправляются чаще раза в 5 минут
- **Rate limiting**: встроенная защита от слишком частых уведомлений

---

## 📱 Что дальше?

### 1. Мониторинг Production

Когда задеплоишь на VPS - получишь уведомления о:
- Запуске/остановке системы
- Всех ошибках в реальном времени
- Проблемах с базой данных
- Медленных запросах

### 2. Health Check мониторинг

Настрой внешний мониторинг (бесплатно):
1. Зарегистрируйся на https://uptimerobot.com (FREE)
2. Добавь монитор: `https://yourdomain.kz/api/health`
3. Настрой проверку каждую минуту
4. Если health check упадет - получишь уведомление в Telegram

### 3. Дневные отчеты

Можешь настроить ежедневные отчеты:
```python
# В Celery task или cron job
await telegram_notifier._send_message(
    f"""
📊 <b>Daily Report</b> - {date}

• Leads created: {leads_count}
• Messages sent: {messages_count}
• Errors: {errors_count}
• Uptime: 99.9%
    """,
    parse_mode="HTML"
)
```

---

## 🔧 Troubleshooting

### Уведомления не приходят

**Проверь:**
1. ✅ TOKEN и CHAT_ID правильные в .env
2. ✅ Написал боту /start
3. ✅ Перезапустил сервер после изменения .env
4. ✅ В логах нет ошибок: `docker-compose logs backend | grep Telegram`

**Проверь через Python:**
```python
import os
print(os.getenv("TELEGRAM_BOT_TOKEN"))  # Должен быть токен
print(os.getenv("TELEGRAM_CHAT_ID"))    # Должен быть chat_id
```

### Приходит слишком много уведомлений

**Решение:**
```python
# Отключи success уведомления в коде
# Оставь только ERROR и CRITICAL

# Увеличь cooldown в shared/telegram_notifier.py:
_notification_cooldown = 600  # 10 минут вместо 5
```

### Хочешь отключить временно

**В .env закомментируй:**
```bash
# TELEGRAM_BOT_TOKEN=...
# TELEGRAM_CHAT_ID=...
```

Система автоматически отключит Telegram и будет просто логировать.

---

## 💰 Стоимость

```
Telegram Bot API: FREE ✅
Telegram уведомления: FREE ✅
Bandwidth: ~1-2MB/день (копейки)
TOTAL: $0/месяц 🎉
```

**VS альтернативы:**
- Sentry: $26/мес
- PagerDuty: $19/мес
- Datadog: $15/мес

---

## 📚 Дополнительные материалы

Примеры кода:
- `shared/telegram_notifier.py` - основной модуль
- `shared/telegram_notifier_example.py` - примеры использования
- `core/api/main.py` - интеграция в FastAPI
- `core/api/routes/health.py` - health check с уведомлениями

---

**Готово! Теперь ты будешь знать о всех проблемах в системе за секунды! 🚀**

Если что-то непонятно - спроси, поможем разобраться!

