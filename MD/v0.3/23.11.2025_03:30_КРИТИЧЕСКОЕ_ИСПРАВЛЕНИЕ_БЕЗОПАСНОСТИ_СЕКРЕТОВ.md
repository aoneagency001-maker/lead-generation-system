# 🔐 Критическое исправление безопасности: Утечка секретов

**Дата создания:** 23.11.2025 03:30  
**Критичность:** 🔴 **ВЫСОКАЯ**

---

## ❗ Проблема

### Обнаружена утечка секретов в публичном репозитории:

1. **Telegram Bot Token** в файле `ENV_TELEGRAM_UPDATE.md` (удалён)
2. GitHub Secret Scanner обнаружил публичную утечку
3. Токен попал в Git историю и остаётся там навсегда (требуется очистка)

### Последствия:

- ✅ Токен Telegram бота скомпрометирован
- ✅ GitHub помечает репозиторий как опасный
- ✅ Автоматические системы могут блокировать CI/CD
- ✅ AWS/Google могут ограничить доступ к API

---

## ✅ Что исправлено

### 1. Удалён токен из файла

**Было:**
```bash
TELEGRAM_MONITOR_BOT_TOKEN=<скрыт_из_соображений_безопасности>
```

**Стало:**
```bash
# ⚠️ ВАЖНО: НЕ КОММИТЬТЕ РЕАЛЬНЫЕ ТОКЕНЫ В GIT!
TELEGRAM_MONITOR_BOT_TOKEN=your_telegram_bot_token_here
```

### 2. Обновлён .gitignore

**Добавлено:**
```
# Files with secrets (NEVER COMMIT)
*_SECRETS.md
*_TOKENS.md
*_CREDENTIALS.md
ENV_*.md
!ENV_EXAMPLE.md
```

### 3. Создан .env.example

Безопасный пример файла с плейсхолдерами вместо реальных значений.

### 4. Обновлён workflow

Добавлена передача секретов через GitHub Secrets.

---

## 🛡️ Что нужно сделать СРОЧНО

### 1. Отозвать скомпрометированный токен Telegram

**Действия:**
1. Откройте: https://t.me/BotFather
2. Отправьте: `/revoke`
3. Выберите бота с скомпрометированным токеном (проверьте в истории Git или в BotFather)
4. Создайте новый токен
5. Добавьте новый токен в GitHub Secrets

### 2. Удалить токен из Git истории

**Вариант 1: Использовать git filter-repo (рекомендуется)**

```bash
# Установить git-filter-repo
pip install git-filter-repo

# Удалить файл из всей истории
git filter-repo --invert-paths --path ENV_TELEGRAM_UPDATE.md

# Или удалить конкретный токен из всех файлов
git filter-repo --replace-text <(echo "7848265878:AAFkCTv7xWNj7Se4RAq8XkMC_7vlxZLT0-k==>REMOVED")
```

**Вариант 2: Использовать BFG Repo-Cleaner**

```bash
# Скачать BFG
wget https://repo1.maven.org/maven2/com/madgag/bfg/1.14.0/bfg-1.14.0.jar

# Удалить токен
java -jar bfg-1.14.0.jar --replace-text tokens.txt

# Создать tokens.txt:
# <токен>==>REMOVED  # Замените на реальный скомпрометированный токен
```

**⚠️ ВАЖНО:** После удаления из истории нужно сделать force push:
```bash
git push origin --force --all
git push origin --force --tags
```

### 3. Добавить секреты в GitHub Secrets

**Settings → Secrets and variables → Actions**

Добавить:
- `TELEGRAM_BOT_TOKEN` - новый токен бота
- `TELEGRAM_MONITOR_BOT_TOKEN` - новый токен мониторинга
- `TELEGRAM_ASSISTANT_BOT_TOKEN` - токен ассистента
- `OPENAI_API_KEY` - ключ OpenAI
- `ANTHROPIC_API_KEY` - ключ Anthropic
- `PERPLEXITY_API_KEY` - ключ Perplexity
- `SUPABASE_SERVICE_KEY` - ключ Supabase
- `YANDEX_METRIKA_TOKEN` - токен Яндекс.Метрики
- `GOOGLE_ANALYTICS_CREDENTIALS` - JSON credentials (base64)

### 4. Обновить локальный .env

Создать новый `.env` файл из `.env.example`:
```bash
cp .env.example .env
# Заполнить реальными значениями (НЕ КОММИТИТЬ!)
```

---

## 📋 Правила безопасности (Cursor Rules)

### ❌ НИКОГДА не делать:

1. ❌ Коммитить реальные токены в .md файлы
2. ❌ Вставлять секреты в документацию
3. ❌ Хранить .env файлы в репозитории
4. ❌ Публиковать credentials.json
5. ❌ Логировать секреты в консоль

### ✅ ВСЕГДА делать:

1. ✅ Использовать GitHub Secrets для production
2. ✅ Создавать .env.example с плейсхолдерами
3. ✅ Добавлять секретные файлы в .gitignore
4. ✅ Проверять Security → Secret Scanning в GitHub
5. ✅ Использовать переменные окружения в коде

---

## 🔍 Проверка других файлов

### Найдены файлы с упоминанием секретов (проверить):

- `telegram-notifications-service/ENV_INFO.md` - проверить на реальные значения
- `telegram-notifications-service/CI_CD_README.md` - проверить на токены
- `scripts/setup-github-secrets.sh` - проверить на хардкод
- Все файлы в `MD/v0.3/` с упоминанием токенов

### Команда для поиска:

```bash
# Найти все файлы с потенциальными токенами
grep -r "TELEGRAM.*TOKEN\|API.*KEY\|SECRET" --include="*.md" --include="*.txt" .

# Найти длинные строки (потенциальные токены)
grep -r "[A-Za-z0-9_-]\{30,\}" --include="*.md" .
```

---

## 🚀 Обновлённый workflow для передачи секретов

### В `.github/workflows/auto-deploy.yml`:

```yaml
- name: 📝 Create .env on server
  run: |
    ssh ${{ env.VPS_USER }}@${{ env.VPS_HOST }} << 'ENDSSH'
      cat > /opt/lead-generation-system/.env << EOF
      # Secrets from GitHub Secrets
      TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_MONITOR_BOT_TOKEN=${{ secrets.TELEGRAM_MONITOR_BOT_TOKEN }}
      OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
      ANTHROPIC_API_KEY=${{ secrets.ANTHROPIC_API_KEY }}
      PERPLEXITY_API_KEY=${{ secrets.PERPLEXITY_API_KEY }}
      SUPABASE_SERVICE_KEY=${{ secrets.SUPABASE_SERVICE_KEY }}
      YANDEX_METRIKA_TOKEN=${{ secrets.YANDEX_METRIKA_TOKEN }}
      EOF
    ENDSSH
```

---

## 📊 Статус исправления

- [x] Удалён токен из файла ENV_TELEGRAM_UPDATE.md
- [x] Обновлён .gitignore
- [x] Создан .env.example
- [ ] Отозван скомпрометированный токен Telegram
- [ ] Удалён токен из Git истории
- [ ] Добавлены секреты в GitHub Secrets
- [ ] Обновлён workflow для передачи секретов
- [ ] Проверены другие файлы на утечки

---

## 🎯 Следующие шаги

1. **СРОЧНО:** Отозвать токен Telegram через BotFather
2. **СРОЧНО:** Удалить токен из Git истории
3. **Важно:** Добавить все секреты в GitHub Secrets
4. **Важно:** Обновить workflow для передачи секретов
5. **Рекомендуется:** Настроить автоматическое сканирование секретов

---

**Дата исправления:** 23.11.2025 03:30  
**Статус:** ⚠️ Требуется немедленное действие

