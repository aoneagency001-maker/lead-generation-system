# ✅ Пагинация и Landing Pages добавлены

**Дата создания:** 22.11.2025 00:01

## 🎯 Что реализовано

### 1. Колонка "Страница входа" в таблице поисковых запросов

**Файл:** `frontend/components/yandex-metrika/AnalyticsDashboard.tsx`

**Добавлено:**
- ✅ Новая колонка "🌐 Страница входа" в таблице ТОП поисковых запросов
- ✅ Ссылки на страницы входа (кликабельные, открываются в новой вкладке)
- ✅ Показ количества дополнительных страниц, если их больше одной
- ✅ Отображение "—" если страниц входа нет

**Формат отображения:**
- Первая страница показывается как ссылка (обрезается до 40 символов если длинная)
- Если страниц больше одной, показывается "+N ещё"
- Если страниц нет, показывается "—"

### 2. Пагинация таблицы поисковых запросов

**Добавлено:**
- ✅ Выбор количества записей на странице: 10, 25, 50
- ✅ Кнопки навигации "Назад" / "Вперёд"
- ✅ Отображение текущей страницы и общего количества
- ✅ Показ диапазона записей (например: "Показано 1–25 из 62")
- ✅ Автоматический сброс на первую страницу при изменении размера страницы

**UI элементы:**
- Dropdown для выбора размера страницы (10/25/50)
- Кнопки навигации с disabled состоянием на первой/последней странице
- Информация о текущей странице и общем количестве

### 3. Backend оптимизация

**Файл:** `core/api/routes/yandex_metrika.py`

**Изменения:**
- ✅ Оптимизирован endpoint `/search-queries-detailed`
- ✅ Landing pages загружаются одним запросом для всех запросов (вместо отдельных запросов для каждого)
- ✅ Используется комбинированный запрос с dimensions: `searchPhrase + URL`
- ✅ Группировка URL по запросам в памяти

**Файл:** `library/integrations/yandex_metrika.py`

**Метод `get_landing_pages_by_search`:**
- ✅ Обновлен для работы с комбинированным запросом
- ✅ Обработка ошибок с возвратом пустого списка
- ✅ Удаление дубликатов URL

### 4. Обновление типов данных

**Файл:** `frontend/components/yandex-metrika/AnalyticsDashboard.tsx`

**Изменения:**
- ✅ Интерфейс `AnalyticsData.queries` обновлен:
  ```typescript
  queries: Array<{ 
    query: string; 
    visits: number; 
    landing_pages: string[] 
  }>
  ```
- ✅ Использование `getMetrikaSearchQueriesDetailed` вместо `getMetrikaSearchQueries`
- ✅ Загрузка до 100 запросов для пагинации

## 📊 Как это работает

### Пагинация

1. Пользователь выбирает размер страницы (10/25/50)
2. Компонент вычисляет:
   - `totalPages = Math.ceil(totalQueries / queriesPageSize)`
   - `startIndex = (queriesPage - 1) * queriesPageSize`
   - `endIndex = startIndex + queriesPageSize`
3. Отображаются только записи в диапазоне `[startIndex, endIndex)`
4. При изменении размера страницы автоматически сбрасывается на первую страницу

### Landing Pages

1. Backend загружает все запросы с их landing pages одним запросом
2. Данные группируются по запросам в памяти
3. Frontend отображает первую страницу как ссылку
4. Если страниц больше одной, показывается счетчик "+N ещё"

## 🎨 UI/UX

**Таблица:**
- Колонка "Страница входа" с кликабельными ссылками
- Обрезка длинных URL (40 символов + "...")
- Tooltip с полным URL при наведении

**Пагинация:**
- Dropdown в заголовке карточки
- Кнопки навигации внизу таблицы
- Информация о текущем диапазоне и общем количестве
- Disabled состояние для кнопок на границах

## ✅ Готово к использованию!

Все функции реализованы и протестированы. Откройте страницу аналитики и наслаждайтесь:
- 📄 Пагинацией с выбором размера страницы
- 🔗 Ссылками на страницы входа для каждого запроса
- 📊 Удобной навигацией по большому количеству запросов

