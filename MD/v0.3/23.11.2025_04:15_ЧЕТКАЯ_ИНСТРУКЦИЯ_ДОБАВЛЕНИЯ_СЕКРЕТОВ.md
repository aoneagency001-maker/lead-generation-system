# 🔐 Чёткая инструкция: Добавление секретов для VPS деплоя

**Дата создания:** 23.11.2025 04:15  
**Статус:** ⚠️ Требуется действие

---

## ❌ Текущая ситуация

**Проверка показала:**
- ❌ `VPS_HOST` - ОТСУТСТВУЕТ
- ❌ `VPS_USER` - ОТСУТСТВУЕТ  
- ❌ `VPS_SSH_KEY` - ОТСУТСТВУЕТ

**Есть только секреты для AWS Lambda:**
- ✅ `AWS_ACCESS_KEY_ID`
- ✅ `AWS_SECRET_ACCESS_KEY`
- ✅ `TELEGRAM_BOT_TOKEN`
- ✅ `TELEGRAM_CHAT_ID`

---

## 🚀 Решение: 3 шага

### Шаг 1: Сгенерировать SSH ключ

```bash
cd /Users/vbut/lead-generation-system
./scripts/generate_ssh_key_for_github_actions.sh
```

**Что делает скрипт:**
1. Создаёт SSH ключ `~/.ssh/github_actions_deploy`
2. Показывает публичный ключ (для VPS)
3. Показывает приватный ключ (для GitHub Secrets)

**Или вручную:**
```bash
# Создать ключ
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy -N ""

# Посмотреть приватный ключ (для GitHub)
cat ~/.ssh/github_actions_deploy

# Посмотреть публичный ключ (для VPS)
cat ~/.ssh/github_actions_deploy.pub
```

---

### Шаг 2: Добавить публичный ключ на VPS

**Нужны данные от тебя:**
- IP адрес или домен VPS: `_____________`
- SSH пользователь (обычно `root` или `ubuntu`): `_____________`

**Команда:**
```bash
# Скопировать публичный ключ на VPS
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub root@<VPS_IP>

# Или если пользователь другой:
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub ubuntu@<VPS_IP>
```

**Если ssh-copy-id не работает:**
```bash
# Вручную добавить ключ на сервер
cat ~/.ssh/github_actions_deploy.pub | ssh root@<VPS_IP> "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

**Проверка подключения:**
```bash
# Должно подключиться без пароля
ssh -i ~/.ssh/github_actions_deploy root@<VPS_IP>
```

---

### Шаг 3: Добавить секреты в GitHub

**Способ 1: Через GitHub UI (рекомендуется)**

1. Открой: `https://github.com/aoneagency001-maker/lead-generation-system/settings/secrets/actions`

2. Нажми **"New repository secret"** для каждого:

#### Секрет 1: VPS_HOST
```
Name: VPS_HOST
Value: <твой_IP_или_домен>
Пример: 123.45.67.89 или vps.example.com
```

#### Секрет 2: VPS_USER
```
Name: VPS_USER
Value: root
(или ubuntu, если используешь Ubuntu)
```

#### Секрет 3: VPS_SSH_KEY
```
Name: VPS_SSH_KEY
Value: (скопируй ВЕСЬ приватный ключ)
```

**Как скопировать ключ:**
```bash
cat ~/.ssh/github_actions_deploy
```

**⚠️ ВАЖНО:** Скопируй ВСЁ, включая:
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
...
(много строк)
...
-----END OPENSSH PRIVATE KEY-----
```

**Способ 2: Через GitHub CLI**

```bash
# Добавить VPS_HOST
echo "123.45.67.89" | gh secret set VPS_HOST --repo aoneagency001-maker/lead-generation-system

# Добавить VPS_USER
echo "root" | gh secret set VPS_USER --repo aoneagency001-maker/lead-generation-system

# Добавить VPS_SSH_KEY
cat ~/.ssh/github_actions_deploy | gh secret set VPS_SSH_KEY --repo aoneagency001-maker/lead-generation-system
```

---

## ✅ Проверка после добавления

```bash
# Проверить что все секреты добавлены
./scripts/check_github_secrets.sh
```

**Должно показать:**
```
✅ VPS_HOST
✅ VPS_USER
✅ VPS_SSH_KEY
```

---

## 🧪 Тестирование workflow

После добавления секретов:

1. Открой: `https://github.com/aoneagency001-maker/lead-generation-system/actions`

2. Выбери workflow **"🚀 Auto Deploy to VPS"**

3. Нажми **"Run workflow"** → **"Run workflow"**

4. Проверь логи:
   - ✅ Шаг "Setup SSH" должен пройти
   - ✅ Шаг "Upload to VPS" должен пройти
   - ✅ Шаг "Deploy on VPS" должен пройти

---

## 📋 Чеклист

- [ ] SSH ключ сгенерирован (`~/.ssh/github_actions_deploy`)
- [ ] Публичный ключ добавлен на VPS (`~/.ssh/authorized_keys`)
- [ ] SSH подключение работает без пароля
- [ ] Секрет `VPS_HOST` добавлен в GitHub
- [ ] Секрет `VPS_USER` добавлен в GitHub
- [ ] Секрет `VPS_SSH_KEY` добавлен в GitHub (полный ключ с BEGIN/END)
- [ ] Проверка секретов прошла успешно (`./scripts/check_github_secrets.sh`)
- [ ] Workflow запущен и прошёл успешно

---

## ❓ Что мне нужно от тебя

**Для полной настройки нужны:**

1. **IP адрес или домен VPS:** `_____________`
2. **SSH пользователь:** `root` или `ubuntu`?
3. **Доступ к VPS:** Можешь подключиться по SSH?

**Если есть доступ к VPS, я могу:**
- Сгенерировать SSH ключ
- Добавить публичный ключ на сервер
- Добавить секреты в GitHub через CLI

**Если нет доступа:**
- Сгенерирую ключ локально
- Дам команды для копирования на сервер
- Добавлю секреты в GitHub

---

## 🔧 Быстрая команда (если есть доступ к VPS)

```bash
# 1. Сгенерировать ключ
./scripts/generate_ssh_key_for_github_actions.sh

# 2. Скопировать на VPS (замени <VPS_IP> и <USER>)
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub <USER>@<VPS_IP>

# 3. Добавить секреты в GitHub (замени <VPS_IP>)
echo "<VPS_IP>" | gh secret set VPS_HOST --repo aoneagency001-maker/lead-generation-system
echo "root" | gh secret set VPS_USER --repo aoneagency001-maker/lead-generation-system
cat ~/.ssh/github_actions_deploy | gh secret set VPS_SSH_KEY --repo aoneagency001-maker/lead-generation-system

# 4. Проверить
./scripts/check_github_secrets.sh
```

---

**Дата создания:** 23.11.2025 04:15  
**Статус:** ⚠️ Ожидает данные VPS от пользователя

