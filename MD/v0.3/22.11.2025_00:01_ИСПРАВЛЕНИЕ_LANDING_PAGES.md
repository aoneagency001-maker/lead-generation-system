# 🔧 Исправление получения Landing Pages

**Дата создания:** 22.11.2025 00:01

## 🎯 Проблема

Landing pages не подтягивались из API Яндекс.Метрики, хотя данные есть в интерфейсе Метрики.

## ✅ Решение

### 1. Обновлен метод `get_search_queries_with_landing_pages`

**Файл:** `library/integrations/yandex_metrika.py`

**Изменения:**
- ✅ Используется комбинация dimensions: `ym:s:searchPhrase` + `ym:s:startURL`
- ✅ Это позволяет получить связку "запрос → страница входа" напрямую из API
- ✅ Запрос возвращает до 10000 строк данных

### 2. Обновлен endpoint `/search-queries-detailed`

**Файл:** `core/api/routes/yandex_metrika.py`

**Изменения:**
- ✅ Использует новый метод `get_search_queries_with_landing_pages`
- ✅ Агрегирует данные по уникальным запросам
- ✅ Группирует landing pages по запросам
- ✅ Сортирует landing pages по популярности (количеству визитов)
- ✅ Fallback: если запрос с landing pages не работает, использует базовый запрос

**Логика агрегации:**
1. Получаем все строки с комбинацией запрос + URL
2. Группируем по запросам
3. Суммируем визиты для каждого запроса
4. Сохраняем все уникальные landing pages для каждого запроса
5. Сортируем landing pages по количеству визитов

### 3. Обработка ошибок

- ✅ Добавлен try-catch для запроса с landing pages
- ✅ Если запрос не работает, используется fallback на базовый запрос
- ✅ Логирование предупреждений для отладки

## 📊 Формат данных

**Входные данные (из API):**
```json
{
  "data": [
    {
      "dimensions": [
        {"name": "vessel group"},
        {"name": "https://example.com/page1"}
      ],
      "metrics": [5, 4]
    }
  ]
}
```

**Выходные данные (обогащенные):**
```json
[
  {
    "query": "vessel group",
    "visits": 5,
    "segment": "Бренд",
    "segments": ["Бренд"],
    "heat_score": 3.5,
    "color": "#cccccc",
    "priority": 0.7,
    "landing_pages": [
      "https://example.com/page1",
      "https://example.com/page2"
    ],
    "first_visit": null,
    "last_visit": null
  }
]
```

## 🔍 Возможные проблемы

### Проблема: Landing pages пустые

**Причины:**
1. API Яндекс.Метрики может не поддерживать комбинацию `ym:s:searchPhrase` + `ym:s:startURL`
2. Для некоторых запросов может не быть данных о landing pages
3. Нужно проверить правильность dimension name

**Решения:**
- ✅ Добавлен fallback на базовый запрос
- ✅ Проверка альтернативных dimensions (`ym:pv:URL`, `ym:s:landingPage`)
- ✅ Логирование для отладки

### Альтернативные dimensions для проверки:

1. `ym:s:startURL` - страница входа (текущий вариант)
2. `ym:pv:URL` - URL просмотра страницы
3. `ym:s:landingPage` - страница входа (альтернативное название)

## 🚀 Следующие шаги

1. **Проверить логи** - посмотреть, что возвращает API при запросе с двумя dimensions
2. **Протестировать альтернативные dimensions** - попробовать `ym:pv:URL` или `ym:s:landingPage`
3. **Проверить документацию API** - убедиться, что комбинация dimensions поддерживается
4. **Добавить фильтры** - возможно, нужно добавить фильтр для поисковых запросов

## ✅ Текущий статус

- ✅ Код обновлен для использования правильного API запроса
- ✅ Добавлена обработка ошибок и fallback
- ✅ Агрегация данных по запросам работает
- ⚠️ Landing pages могут быть пустыми (требуется проверка API)

## 📝 Примечания

Если landing pages все еще пустые, возможно нужно:
1. Использовать Logs API вместо Reporting API для получения детальных данных
2. Делать отдельные запросы для каждого запроса (менее эффективно)
3. Проверить настройки счетчика в Яндекс.Метрике

