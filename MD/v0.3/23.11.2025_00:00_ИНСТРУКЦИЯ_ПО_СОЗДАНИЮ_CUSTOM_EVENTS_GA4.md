# Инструкция по созданию Custom Events в GA4

**Дата создания:** 23.11.2025 00:00

## 🎯 Цель

Автоматически зарегистрировать все custom events из гайда `GA4_Custom_Events_Full_Guide.md` в вашем GA4 Property.

## 📋 Подготовка

### Шаг 1: Получить GA4 Measurement ID

1. Откройте GA4: https://analytics.google.com/
2. Выберите ваш Property
3. Перейдите в **Admin → Data Streams**
4. Выберите ваш Data Stream (обычно Web)
5. Скопируйте **Measurement ID** (формат: `G-XXXXXXXXXX`)

### Шаг 2: Создать API Secret

1. В том же Data Stream нажмите **"Measurement Protocol API secrets"**
2. Нажмите **"Create"**
3. Введите название (например: "Script Registration")
4. Скопируйте созданный **API Secret** (формат: длинная строка)

### Шаг 3: Добавить в .env

Откройте `.env` и добавьте:

```env
# GA4 для регистрации событий
GA4_MEASUREMENT_ID=G-XXXXXXXXXX
GA4_API_SECRET=your_api_secret_here
```

**⚠️ ВАЖНО:** Не коммитьте `.env` в Git!

## 🚀 Использование скрипта

### Базовое использование

```bash
python scripts/create_ga4_custom_events.py
```

Скрипт автоматически:
- Загрузит `GA4_MEASUREMENT_ID` и `GA4_API_SECRET` из `.env`
- Отправит все события из гайда через Measurement Protocol
- События зарегистрируются в GA4 через 2-5 минут

### Параметры командной строки

```bash
# Указать Measurement ID и API Secret напрямую
python scripts/create_ga4_custom_events.py \
  --measurement-id G-XXXXXXXXXX \
  --api-secret your_secret

# Создать только Tier 1 события (критические)
python scripts/create_ga4_custom_events.py --tier "Tier 1"

# Показать список событий без отправки (dry run)
python scripts/create_ga4_custom_events.py --dry-run
```

### Фильтрация по Tier

```bash
# Только критичные события
python scripts/create_ga4_custom_events.py --tier "Tier 1"

# Только важные события
python scripts/create_ga4_custom_events.py --tier "Tier 2"

# Только дополнительные события
python scripts/create_ga4_custom_events.py --tier "Tier 3"
```

## 📊 Список событий

### Tier 1: CRITICAL (19 событий)

**Conversion Events:**
- `form_submit` - Форма успешно отправлена
- `lead_qualified` - Лид прошёл валидацию
- `booking_confirmed` - Бронь подтверждена
- `contact_created` - Контакт создан в CRM

**Engagement Events:**
- `form_field_focus` - Фокус на поле формы
- `form_field_fill` - Заполнение поля формы
- `form_error` - Ошибка валидации формы
- `phone_view` - Просмотр номера телефона
- `email_copy` - Копирование email
- `cta_click` - Клик на CTA кнопку

**Quality Events:**
- `time_on_page` - Время на странице
- `scroll_depth` - Глубина скролла
- `video_play` - Воспроизведение видео
- `document_download` - Скачивание документа
- `content_view` - Просмотр контента

**Bot Events:**
- `telegram_message` - Сообщение в Telegram боте
- `user_intent_detected` - Обнаружен intent пользователя
- `contact_shared` - Контакт поделен в Telegram

### Tier 2: IMPORTANT (15 событий)

**Lead Source:**
- `utm_source_track`, `utm_medium_track`, `utm_campaign_track`, `referrer_track`

**Segment:**
- `device_info`, `browser_type`, `os_type`, `location_detect`, `language_detect`

**Intent:**
- `search_query`, `product_view`, `comparison_view`, `wishlist_add`, `cart_activity`

### Tier 3: NICE-TO-HAVE (9 событий)

**Retention:**
- `repeat_visit`, `days_since_first`, `session_count`, `return_time`

**Micro-Conversions:**
- `newsletter_signup`, `demo_request`, `webinar_register`, `ebook_download`, `coupon_apply`

**Всего: 43 события**

## ✅ Проверка результатов

### 1. Real-time отчет

1. Откройте GA4 → **Reports → Real-time**
2. Подождите 2-5 минут после запуска скрипта
3. Проверьте, что события появляются в списке

### 2. Events список

1. Откройте GA4 → **Admin → Events**
2. Найдите созданные события в списке
3. Отметьте важные события как **Conversions** (если нужно)

### 3. Custom Definitions

После регистрации событий можно создать Custom Dimensions для параметров:

1. GA4 → **Admin → Custom Definitions**
2. Создайте Dimensions для параметров событий
3. Например: `form_name`, `hot_score`, `scroll_percent`

## 🔧 Устранение проблем

### Ошибка: "GA4_MEASUREMENT_ID не установлен"

**Решение:**
- Убедитесь, что `GA4_MEASUREMENT_ID` указан в `.env`
- Или передайте через `--measurement-id`

### Ошибка: "GA4_API_SECRET не установлен"

**Решение:**
- Создайте API Secret в GA4 (Admin → Data Streams → Measurement Protocol API secrets)
- Добавьте в `.env` как `GA4_API_SECRET`

### Ошибка: "401 Unauthorized"

**Решение:**
- Проверьте правильность `GA4_MEASUREMENT_ID` и `GA4_API_SECRET`
- Убедитесь, что API Secret активен (не удален)

### События не появляются в GA4

**Решение:**
- Подождите 2-5 минут (GA4 обрабатывает события с задержкой)
- Проверьте Real-time отчет
- Убедитесь, что Measurement ID правильный

## 📝 Следующие шаги

После регистрации событий:

1. **Настройте конверсии:**
   - GA4 → Admin → Conversions
   - Отметьте важные события как конверсии

2. **Создайте Custom Dimensions:**
   - Для параметров событий (form_name, hot_score, etc.)

3. **Настройте Measurement Protocol в коде:**
   - Используйте события в вашем приложении
   - Отправляйте реальные события через Measurement Protocol

4. **Создайте отчеты:**
   - Используйте события в Exploration отчетах
   - Настройте дашборды

## 🔗 Связанные документы

- `GA4_Custom_Events_Full_Guide.md` - Полный гайд по событиям
- `22.11.2025_18:05_ОТЧЕТ_О_ГОТОВНОСТИ_МОДУЛЯ_GA4.md` - Отчет о готовности GA4 модуля

---

**Готово!** После запуска скрипта все события будут зарегистрированы в GA4. 🚀

