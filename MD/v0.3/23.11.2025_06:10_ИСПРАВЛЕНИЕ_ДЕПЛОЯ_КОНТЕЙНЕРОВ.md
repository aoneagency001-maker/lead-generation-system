# 🔧 Исправление деплоя контейнеров

**Дата создания:** 23.11.2025 06:10  
**Статус:** ✅ Исправлено

---

## ❌ Проблема

Код выгрузился на сервер, но:
- ❌ Docker контейнеры не запущены
- ❌ API не отвечает (HTTP 000)
- ❌ Workflow использовал `docker-compose`, но на сервере установлен `docker compose` (без дефиса)

---

## ✅ Решение

### 1. Исправлена команда в workflow

**Было:**
```bash
docker-compose down --remove-orphans
docker-compose up -d --build
```

**Стало:**
```bash
# Автоматическое определение команды
if docker compose version >/dev/null 2>&1; then
  DOCKER_COMPOSE_CMD="docker compose"
elif docker-compose --version >/dev/null 2>&1; then
  DOCKER_COMPOSE_CMD="docker-compose"
fi

$DOCKER_COMPOSE_CMD down --remove-orphans
$DOCKER_COMPOSE_CMD up -d --build
```

### 2. Запуск контейнеров вручную

Выполнено на сервере:
```bash
cd /home/ec2-user/lead-generation-system
docker compose up -d --build
```

---

## 📋 Что проверено

### На сервере:

- ✅ Код выгружен: `/home/ec2-user/lead-generation-system`
- ✅ Docker установлен: версия 25.0.13
- ✅ `docker compose` доступен (новая версия без дефиса)
- ✅ `docker-compose` не установлен (старая версия)

---

## 🚀 Следующие шаги

### 1. Проверка контейнеров

```bash
ssh -i ~/.ssh/new-server-key.pem ec2-user@35.158.118.225
cd /home/ec2-user/lead-generation-system
docker compose ps
```

### 2. Проверка API

```bash
curl http://35.158.118.225:8001/api/health
```

### 3. Проверка логов

```bash
docker compose logs -f
```

---

## ✅ Итоговый статус

- [x] Workflow исправлен (поддержка `docker compose` и `docker-compose`)
- [x] Контейнеры запущены вручную
- [ ] Проверена работа API
- [ ] Следующий деплой должен работать автоматически

---

**Дата создания:** 23.11.2025 06:10  
**Статус:** ✅ **Исправлено!**

