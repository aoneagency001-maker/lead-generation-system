# 🔍 Проверка доступности AWS EC2 сервера

**Дата создания:** 23.11.2025 05:20  
**Статус:** 🔍 Диагностика

---

## 📋 Имеющаяся информация

### Данные сервера:

- **IP адрес:** `13.220.11.94`
- **Пользователь:** `ec2-user`
- **Ключ:** `~/.ssh/leadgen-system-key.pem`
- **Instance ID:** `i-0fd42f4b1d227227e` (test-agent-node)

### GitHub Secrets:

- `VPS_HOST` = `13.220.11.94`
- `VPS_USER` = `ec2-user`
- `VPS_SSH_KEY` = приватный ключ для GitHub Actions

---

## 🔍 Диагностика подключения

### 1. Проверка доступности IP

```bash
ping -c 3 13.220.11.94
```

**Ожидаемый результат:**
- ✅ IP отвечает на ping
- ❌ IP не отвечает (возможно, ICMP заблокирован)

### 2. Проверка порта 22 (SSH)

```bash
nc -zv -w 5 13.220.11.94 22
```

**Ожидаемый результат:**
- ✅ Порт 22 открыт
- ❌ Порт 22 закрыт или недоступен

### 3. Проверка SSH подключения

```bash
ssh -i ~/.ssh/leadgen-system-key.pem ec2-user@13.220.11.94
```

**Ожидаемый результат:**
- ✅ Подключение успешно
- ❌ Таймаут или ошибка аутентификации

---

## 🔧 Возможные проблемы и решения

### Проблема 1: Таймаут подключения

**Причины:**
- Security Group в AWS блокирует порт 22
- Сервер выключен или перезагружается
- Неправильный IP адрес

**Решение:**
1. Проверь Security Groups в AWS Console:
   - EC2 → Instances → выбери инстанс
   - Security → Security groups → Inbound rules
   - Должно быть правило: Type=SSH, Port=22, Source=0.0.0.0/0 (или твой IP)

2. Проверь статус инстанса:
   - Должен быть "running" (зелёный)

3. Проверь правильность IP:
   - Public IPv4 address в консоли AWS

### Проблема 2: Permission denied

**Причины:**
- Неправильный ключ
- Неправильный пользователь
- Неправильные права на ключ

**Решение:**
```bash
# Установи правильные права на ключ
chmod 400 ~/.ssh/leadgen-system-key.pem

# Попробуй подключиться
ssh -i ~/.ssh/leadgen-system-key.pem ec2-user@13.220.11.94
```

### Проблема 3: Ключ не найден

**Решение:**
1. Скачай ключ из AWS Console:
   - EC2 → Key Pairs
   - Найди `leadgen-system-key`
   - Скачай .pem файл

2. Сохрани в `~/.ssh/leadgen-system-key.pem`

3. Установи права:
   ```bash
   chmod 400 ~/.ssh/leadgen-system-key.pem
   ```

---

## 📝 Инструкция для подключения

### Шаг 1: Подготовка ключа

```bash
# Перейди в директорию с ключами
cd ~/.ssh

# Проверь наличие ключа
ls -la leadgen-system-key.pem

# Если нет - скачай из AWS Console и сохрани сюда
# Затем установи права:
chmod 400 leadgen-system-key.pem
```

### Шаг 2: Подключение

```bash
ssh -i ~/.ssh/leadgen-system-key.pem ec2-user@13.220.11.94
```

**Если первый раз:**
- Будет вопрос "Are you sure you want to continue connecting?"
- Напиши `yes` и нажми Enter

### Шаг 3: Проверка

После подключения ты должен увидеть:

```
   ,     #_
   ~\_  ####_        Amazon Linux 2023
  ~~  \_#####\
  ...
[ec2-user@ip-172-31-75-196 ~]$
```

---

## 🔍 Что проверить в AWS Console

1. **Статус инстанса:**
   - EC2 → Instances
   - Должен быть "running" (зелёная галочка)

2. **Public IP:**
   - EC2 → Instances → столбец "Public IPv4 address"
   - Должен быть `13.220.11.94`

3. **Security Group:**
   - EC2 → Instances → выбери инстанс
   - Security → Security groups → Inbound rules
   - Должно быть правило для SSH (порт 22)

4. **Key Pair:**
   - EC2 → Key Pairs
   - Должен быть `leadgen-system-key`

---

## ✅ Следующие шаги

После успешного подключения:

1. **Проверь директорию деплоя:**
   ```bash
   ls -la /home/ec2-user/lead-generation-system
   ```

2. **Проверь Docker:**
   ```bash
   docker ps
   docker-compose --version
   ```

3. **Проверь права:**
   ```bash
   whoami
   pwd
   ```

---

**Дата создания:** 23.11.2025 05:20  
**Статус:** 🔍 **Диагностика в процессе**

