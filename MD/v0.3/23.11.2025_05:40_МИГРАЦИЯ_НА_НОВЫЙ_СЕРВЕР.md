# 🚀 Миграция на новый сервер

**Дата создания:** 23.11.2025 05:40  
**Статус:** 🔄 В процессе

---

## 📋 Информация о новом сервере

### Данные сервера:

- **IP адрес:** `172.31.35.236` (внутренний IP)
- **Hostname:** `ip-172-31-35-236`
- **Пользователь:** `ec2-user`
- **Ключ:** `~/Downloads/21.pem` → `~/.ssh/new-server-key.pem`

### ⚠️ Важно:

IP `172.31.35.236` - это **внутренний (private) IP**. Для SSH подключения извне нужен **публичный (public) IP**.

**Что нужно сделать:**
1. Найди Public IPv4 address в AWS Console
2. Обнови `VPS_HOST` в GitHub Secrets на публичный IP

---

## ✅ Что сделано

### 1. Ключ скопирован

- **Источник:** `~/Downloads/21.pem`
- **Назначение:** `~/.ssh/new-server-key.pem`
- **Права:** `400` (r--------)

### 2. GitHub Secrets обновлены

- ✅ `VPS_HOST` = `172.31.35.236` (временно, нужен публичный IP)
- ✅ `VPS_USER` = `ec2-user`
- ✅ `VPS_SSH_KEY` = новый приватный ключ

---

## 🔍 Проверка подключения

### Проблема с внутренним IP:

IP `172.31.35.236` - это внутренний IP адрес AWS. Для SSH подключения извне (из интернета) нужен **публичный IP**.

### Как найти публичный IP:

1. Зайди в AWS Console → EC2 → Instances
2. Найди инстанс с внутренним IP `172.31.35.236`
3. Посмотри столбец **"Public IPv4 address"**
4. Это и есть публичный IP для SSH

---

## 🔧 Следующие шаги

### Шаг 1: Найти публичный IP

В AWS Console:
- EC2 → Instances
- Найди инстанс с IP `172.31.35.236`
- Скопируй **Public IPv4 address**

### Шаг 2: Обновить VPS_HOST

```bash
gh secret set VPS_HOST --repo aoneagency001-maker/lead-generation-system --body "ПУБЛИЧНЫЙ_IP"
```

### Шаг 3: Проверить подключение

```bash
ssh -i ~/.ssh/new-server-key.pem ec2-user@ПУБЛИЧНЫЙ_IP
```

### Шаг 4: Обновить workflow (если нужно)

Если публичный IP отличается, workflow автоматически использует новое значение из GitHub Secrets.

---

## 📝 Команды для подключения

### С внутренним IP (работает только изнутри AWS):

```bash
ssh -i ~/.ssh/new-server-key.pem ec2-user@172.31.35.236
```

### С публичным IP (работает из интернета):

```bash
ssh -i ~/.ssh/new-server-key.pem ec2-user@ПУБЛИЧНЫЙ_IP
```

---

## ✅ Итоговый статус

- [x] Ключ скопирован в `~/.ssh/new-server-key.pem`
- [x] GitHub Secrets обновлены (VPS_HOST, VPS_USER, VPS_SSH_KEY)
- [ ] Найден публичный IP адрес
- [ ] Обновлён VPS_HOST на публичный IP
- [ ] Проверено SSH подключение
- [ ] Workflow протестирован

---

## 🔍 Диагностика

### Если подключение не работает:

1. **Проверь, что используешь публичный IP:**
   - Внутренний IP работает только изнутри AWS
   - Для внешнего доступа нужен публичный IP

2. **Проверь Security Groups:**
   - Порт 22 должен быть открыт для 0.0.0.0/0 (или твой IP)

3. **Проверь ключ:**
   ```bash
   ssh -i ~/.ssh/new-server-key.pem -v ec2-user@ПУБЛИЧНЫЙ_IP
   ```

---

**Дата создания:** 23.11.2025 05:40  
**Статус:** 🔄 **Ожидается публичный IP адрес**

