# 🔧 Инструкция по SSH подключению к серверу

**Дата создания:** 23.11.2025 05:35  
**Статус:** 📝 Инструкция

---

## 📋 Команда для подключения

```bash
ssh -i "/Users/vbut/Downloads/New leaven system key.pem" ec2-user@ec2-13-220-11-94.compute-1.amazonaws.com
```

---

## 🔍 Что происходит при подключении

### Нормальное поведение:

1. **Первое подключение:**
   ```
   The authenticity of host 'ec2-13-220-11-94.compute-1.amazonaws.com (13.220.11.94)' can't be established.
   ECDSA key fingerprint is SHA256:...
   Are you sure you want to continue connecting (yes/no/[fingerprint])? 
   ```
   - Напиши `yes` и нажми Enter

2. **После подтверждения:**
   ```
   [ec2-user@ip-172-31-75-196 ~]$
   ```
   - Ты подключён к серверу!
   - Можешь выполнять команды

### Если "ничего не происходит":

**Возможные причины:**

1. **SSH зависает на подключении**
   - Подожди 10-30 секунд
   - Если ничего не происходит - нажми `Ctrl+C` для отмены

2. **SSH подключается, но сессия сразу закрывается**
   - Проверь логи выше
   - Возможно, проблема с SSH сервисом на сервере

3. **Терминал не показывает приглашение**
   - Попробуй нажать Enter
   - Или выполни команду: `whoami`

---

## 🔧 Решения проблем

### Проблема 1: SSH зависает

**Решение:**

```bash
# Добавь таймаут и verbose режим
ssh -i "/Users/vbut/Downloads/New leaven system key.pem" \
    -o ConnectTimeout=30 \
    -v \
    ec2-user@ec2-13-220-11-94.compute-1.amazonaws.com
```

**Что делать:**
- Подожди 30 секунд
- Если не работает - нажми `Ctrl+C`
- Проверь логи (буква `v` показывает подробности)

### Проблема 2: "Permission denied"

**Решение:**

```bash
# Установи правильные права на ключ
chmod 400 "/Users/vbut/Downloads/New leaven system key.pem"

# Попробуй снова
ssh -i "/Users/vbut/Downloads/New leaven system key.pem" \
    ec2-user@ec2-13-220-11-94.compute-1.amazonaws.com
```

### Проблема 3: "Connection timed out"

**Решение:**

1. Проверь, что сервер запущен в AWS Console
2. Проверь Security Groups (порт 22 должен быть открыт)
3. Попробуй другой способ подключения (Session Manager)

---

## ✅ Правильная команда с опциями

```bash
ssh -i "/Users/vbut/Downloads/New leaven system key.pem" \
    -o ConnectTimeout=30 \
    -o ServerAliveInterval=10 \
    -o StrictHostKeyChecking=no \
    ec2-user@ec2-13-220-11-94.compute-1.amazonaws.com
```

**Что делают опции:**
- `ConnectTimeout=30` - таймаут подключения 30 секунд
- `ServerAliveInterval=10` - отправлять keepalive каждые 10 секунд
- `StrictHostKeyChecking=no` - не спрашивать подтверждение хоста

---

## 🧪 Тест подключения

### Быстрый тест (выполнить команду и выйти):

```bash
ssh -i "/Users/vbut/Downloads/New leaven system key.pem" \
    -o ConnectTimeout=10 \
    ec2-user@ec2-13-220-11-94.compute-1.amazonaws.com \
    "echo '✅ Подключение работает!' && whoami && hostname"
```

**Ожидаемый результат:**
```
✅ Подключение работает!
ec2-user
ip-172-31-75-196.ec2.internal
```

---

## 📝 Пошаговая инструкция

### Шаг 1: Открой терминал

- На Mac: `Cmd + Space` → введи "Terminal" → Enter

### Шаг 2: Выполни команду

```bash
ssh -i "/Users/vbut/Downloads/New leaven system key.pem" ec2-user@ec2-13-220-11-94.compute-1.amazonaws.com
```

### Шаг 3: Если спросит подтверждение

```
Are you sure you want to continue connecting (yes/no)?
```

Напиши `yes` и нажми Enter

### Шаг 4: Если подключился

Ты увидишь:
```
[ec2-user@ip-172-31-75-196 ~]$
```

**Поздравляю! Ты подключён к серверу!**

### Шаг 5: Проверь подключение

Выполни команды:
```bash
whoami    # Должно показать: ec2-user
pwd       # Должно показать: /home/ec2-user
hostname  # Должно показать: ip-172-31-75-196.ec2.internal
```

### Шаг 6: Выход из сервера

Нажми `Ctrl+D` или выполни:
```bash
exit
```

---

## 🔍 Диагностика

### Если команда "ничего не делает":

1. **Проверь, что команда выполняется:**
   - Терминал должен показывать курсор
   - Попробуй нажать Enter несколько раз

2. **Проверь подключение с verbose:**
   ```bash
   ssh -i "/Users/vbut/Downloads/New leaven system key.pem" \
       -v \
       ec2-user@ec2-13-220-11-94.compute-1.amazonaws.com
   ```

3. **Проверь права на ключ:**
   ```bash
   ls -la "/Users/vbut/Downloads/New leaven system key.pem"
   # Должно быть: -r-------- (права 400)
   ```

4. **Проверь доступность сервера:**
   ```bash
   ping -c 3 13.220.11.94
   ```

---

## ✅ Итоговая команда (рекомендуемая)

```bash
ssh -i "/Users/vbut/Downloads/New leaven system key.pem" \
    -o ConnectTimeout=30 \
    -o ServerAliveInterval=10 \
    -o StrictHostKeyChecking=no \
    ec2-user@ec2-13-220-11-94.compute-1.amazonaws.com
```

**Скопируй и вставь в терминал!**

---

**Дата создания:** 23.11.2025 05:35  
**Статус:** 📝 **Инструкция готова**

