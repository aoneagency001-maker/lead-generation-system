# Telegram Bot - Автоматическое сохранение подписчиков

**Дата создания:** 19.11.2025 23:55

## 🎯 Что это?

Теперь когда пользователь нажимает `/start` в Telegram боте, его **chat_id автоматически сохраняется в базу данных** с указанием к какому боту он подписан.

### ✅ Что получаешь:

- **Автоматическое сохранение** chat_id при `/start`
- **Идентификация бота** - понимаешь какой chat_id к какому боту относится
- **Информация о пользователе** - username, имя, язык
- **Отслеживание активности** - когда последний раз был активен
- **Статусы подписчиков** - active, blocked, unsubscribed

---

## 📊 Структура базы данных

### Таблица: `telegram_bot_subscribers`

```sql
CREATE TABLE telegram_bot_subscribers (
    id UUID PRIMARY KEY,
    bot_type VARCHAR(50) NOT NULL,  -- monitor, leads, sales и т.д.
    chat_id BIGINT NOT NULL,         -- Telegram Chat ID
    username TEXT,                   -- @username
    first_name TEXT,                 -- Имя
    last_name TEXT,                  -- Фамилия
    language_code VARCHAR(10),        -- ru, en, kk
    status VARCHAR(50) DEFAULT 'active',
    metadata JSONB,
    subscribed_at TIMESTAMP,
    last_activity_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

**Уникальность:** Один `chat_id` может быть подписан на один `bot_type` только один раз.

---

## 🚀 Установка

### Шаг 1: Применить схему базы данных

**Вариант 1: Через Supabase Dashboard (рекомендуется)**

1. Открой Supabase Dashboard → SQL Editor
2. Скопируй содержимое файла: `core/database/schema_telegram_bots.sql`
3. Вставь и выполни

**Вариант 2: Через psql**

```bash
psql $DATABASE_URL -f core/database/schema_telegram_bots.sql
```

**Вариант 3: Через скрипт**

```bash
python scripts/apply_telegram_bots_schema.py
```

Скрипт покажет инструкции для ручного применения.

---

### Шаг 2: Проверь что схема применена

```sql
-- В Supabase SQL Editor
SELECT * FROM telegram_bot_subscribers LIMIT 1;
```

Если таблица существует - готово! ✅

---

## 🧪 Тестирование

### Тест 1: Подписка через бота

1. Запусти бота:
```bash
python -m shared.telegram_bot
```

2. Открой бота в Telegram и отправь `/start`

3. Проверь базу данных:
```sql
SELECT * FROM telegram_bot_subscribers 
WHERE bot_type = 'monitor' 
ORDER BY subscribed_at DESC 
LIMIT 5;
```

Должен появиться твой chat_id! ✅

---

### Тест 2: Проверка сохранения данных

```python
# test_subscriber_save.py
import asyncio
from core.database.supabase_client import get_supabase_client

async def test():
    supabase = get_supabase_client()
    
    # Получить всех подписчиков monitor бота
    result = supabase.table("telegram_bot_subscribers").select("*").eq(
        "bot_type", "monitor"
    ).execute()
    
    print(f"✅ Found {len(result.data)} subscribers:")
    for sub in result.data:
        print(f"  - Chat ID: {sub['chat_id']}")
        print(f"    Username: @{sub.get('username', 'N/A')}")
        print(f"    Name: {sub.get('first_name', 'N/A')}")
        print(f"    Status: {sub['status']}")
        print(f"    Subscribed: {sub['subscribed_at']}")
        print()

asyncio.run(test())
```

---

## 📱 Как это работает

### 1. Пользователь нажимает `/start`

```
Пользователь → /start → Telegram Bot
```

### 2. Бот сохраняет данные

```python
# В shared/telegram_bot.py
async def _handle_update(self, update):
    if command == "/start":
        # Автоматически сохраняет в БД
        await self._save_subscriber(user_data)
```

### 3. Данные в базе

```json
{
  "bot_type": "monitor",
  "chat_id": 280192618,
  "username": "nikolai_user",
  "first_name": "Nikolai",
  "status": "active",
  "subscribed_at": "2025-11-19T23:55:00Z"
}
```

---

## 🔍 Запросы к базе данных

### Получить всех подписчиков monitor бота

```sql
SELECT 
    chat_id,
    username,
    first_name,
    status,
    subscribed_at,
    last_activity_at
FROM telegram_bot_subscribers
WHERE bot_type = 'monitor'
ORDER BY subscribed_at DESC;
```

### Получить активных подписчиков

```sql
SELECT * FROM telegram_bot_subscribers
WHERE bot_type = 'monitor' 
  AND status = 'active'
ORDER BY last_activity_at DESC;
```

### Статистика по ботам

```sql
SELECT 
    bot_type,
    COUNT(*) as total_subscribers,
    COUNT(*) FILTER (WHERE status = 'active') as active_subscribers,
    MAX(subscribed_at) as last_subscription
FROM telegram_bot_subscribers
GROUP BY bot_type;
```

### Найти подписчика по chat_id

```sql
SELECT * FROM telegram_bot_subscribers
WHERE chat_id = 280192618;
```

---

## 🎨 Использование в коде

### Отправить уведомление всем подписчикам

```python
from core.database.supabase_client import get_supabase_client
from shared.telegram_notifier import TelegramErrorNotifier

async def notify_all_subscribers(bot_type: str, message: str):
    """Отправить сообщение всем подписчикам бота"""
    supabase = get_supabase_client()
    
    # Получить всех активных подписчиков
    result = supabase.table("telegram_bot_subscribers").select(
        "chat_id"
    ).eq("bot_type", bot_type).eq("status", "active").execute()
    
    # Отправить каждому
    for subscriber in result.data:
        chat_id = subscriber["chat_id"]
        notifier = TelegramErrorNotifier(bot_token, chat_id)
        await notifier.send_success(message, module="Broadcast")
```

---

### Получить список подписчиков для админки

```python
async def get_subscribers_list(bot_type: str):
    """Получить список подписчиков для админки"""
    supabase = get_supabase_client()
    
    result = supabase.table("telegram_bot_subscribers").select(
        "chat_id, username, first_name, status, subscribed_at, last_activity_at"
    ).eq("bot_type", bot_type).order("subscribed_at", desc=True).execute()
    
    return result.data
```

---

## 🔧 Типы ботов

### Текущие типы:

- `monitor` - Бот для мониторинга системы и ошибок
- `leads` - Бот для уведомлений о новых лидах (будущее)
- `sales` - Бот для продаж (будущее)
- `qualification` - Бот для квалификации лидов (будущее)

### Как добавить новый тип бота:

1. Создай нового бота через @BotFather
2. Добавь токен в `.env`:
```bash
TELEGRAM_LEADS_BOT_TOKEN=новый_токен
```

3. Запусти бота с указанием типа:
```python
from shared.telegram_bot import TelegramBot

bot = TelegramBot(bot_type="leads")
await bot.start_polling()
```

4. Подписчики автоматически сохранятся с `bot_type="leads"`

---

## 📊 Мониторинг подписчиков

### Команда для админа: `/subscribers`

Можешь добавить команду для просмотра подписчиков:

```python
async def _cmd_subscribers(self, chat_id: int):
    """Показать список подписчиков (только для админа)"""
    # Проверка что это админ
    if str(chat_id) != str(self.admin_chat_id):
        await self._send_message(chat_id, "❌ Access denied")
        return
    
    supabase = get_supabase_client()
    result = supabase.table("telegram_bot_subscribers").select(
        "*"
    ).eq("bot_type", self.bot_type).order("subscribed_at", desc=True).limit(10).execute()
    
    message = f"📊 Subscribers ({self.bot_type}):\n\n"
    for sub in result.data:
        message += f"• @{sub.get('username', 'N/A')} ({sub['chat_id']})\n"
        message += f"  Status: {sub['status']}\n"
        message += f"  Subscribed: {sub['subscribed_at']}\n\n"
    
    await self._send_message(chat_id, message)
```

---

## 🛡️ Безопасность

### Защита от спама

- Один `chat_id` может подписаться на один `bot_type` только один раз
- При повторном `/start` - данные обновляются (upsert)

### Статусы подписчиков

- `active` - Активный подписчик
- `blocked` - Заблокирован администратором
- `unsubscribed` - Отписался
- `banned` - Забанен

### Обновление статуса

```python
# Заблокировать подписчика
supabase.table("telegram_bot_subscribers").update({
    "status": "blocked"
}).eq("bot_type", "monitor").eq("chat_id", chat_id).execute()
```

---

## 📈 Аналитика

### Статистика подписок

```sql
-- Подписчики по дням
SELECT 
    DATE(subscribed_at) as date,
    COUNT(*) as new_subscribers
FROM telegram_bot_subscribers
WHERE bot_type = 'monitor'
GROUP BY DATE(subscribed_at)
ORDER BY date DESC
LIMIT 30;
```

### Активность подписчиков

```sql
-- Самые активные подписчики
SELECT 
    chat_id,
    username,
    COUNT(*) as activity_count,
    MAX(last_activity_at) as last_seen
FROM telegram_bot_subscribers
WHERE bot_type = 'monitor'
  AND status = 'active'
GROUP BY chat_id, username
ORDER BY last_activity_at DESC
LIMIT 10;
```

---

## ✅ Checklist

- [ ] Схема базы данных применена
- [ ] Бот запущен: `python -m shared.telegram_bot`
- [ ] Отправлен `/start` боту
- [ ] Проверено что chat_id сохранился в БД
- [ ] Проверены запросы к базе данных

---

## 🐛 Troubleshooting

### Подписчик не сохраняется

**Проверь:**
1. ✅ Схема применена: `SELECT * FROM telegram_bot_subscribers LIMIT 1;`
2. ✅ Бот запущен и работает
3. ✅ Логи бота показывают сохранение
4. ✅ Нет ошибок в логах

**Логи:**
```bash
# Проверь логи бота
python -m shared.telegram_bot
# Должно быть: "✅ Subscriber saved: chat_id=..."
```

---

### Ошибка "relation does not exist"

**Решение:**
1. Примени схему вручную через Supabase Dashboard
2. Или через psql: `psql $DATABASE_URL -f core/database/schema_telegram_bots.sql`

---

## 📚 Файлы

- `core/database/schema_telegram_bots.sql` - SQL схема таблицы
- `shared/telegram_bot.py` - Код бота с сохранением подписчиков
- `scripts/apply_telegram_bots_schema.py` - Скрипт для применения схемы

---

**Готово! Теперь все подписчики автоматически сохраняются в базу данных! 🚀**


