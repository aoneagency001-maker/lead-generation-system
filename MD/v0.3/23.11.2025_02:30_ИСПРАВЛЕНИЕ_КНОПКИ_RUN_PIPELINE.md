# 🔧 Исправление кнопки "Run Pipeline"

**Дата создания:** 23.11.2025 02:30

## ❌ Проблема

Кнопка "Run Pipeline" была disabled и не работала при нажатии.

**Причина:**
- Кнопка была disabled когда `status?.overall_status !== 'ready'`
- API возвращает `"partial"` если не все API ключи настроены
- Это блокировало запуск даже когда часть сервисов доступна

## ✅ Исправления

### 1. Изменено условие disabled

**Было:**
```typescript
disabled={running || status?.overall_status !== 'ready'}
```

**Стало:**
```typescript
disabled={running || !status}
```

Теперь кнопка активна даже при `"partial"` статусе (когда не все API ключи настроены, но часть сервисов доступна).

### 2. Добавлена отладочная информация

Добавлены `console.log` для отладки:
- Статус LLM сервисов при загрузке
- Параметры запроса при запуске pipeline
- Ответ от API
- Ошибки с детальной информацией

### 3. Улучшена обработка ошибок

- Более детальные сообщения об ошибках
- Логирование всех ошибок в консоль
- Показ ошибок пользователю

### 4. Добавлена визуальная подсказка

Показывается подсказка "(Partial: Some APIs not configured)" когда статус partial.

## 🔍 Как проверить отладчик

### В браузере (Chrome/Edge/Firefox)

1. **Откройте DevTools:**
   - `F12` или `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
   - Или правый клик → "Inspect" → вкладка "Console"

2. **Проверьте консоль:**
   - Должны быть логи: `LLM Status: {...}`
   - При нажатии на кнопку: `Running pipeline: {...}`
   - Ответ API: `Pipeline response: ...`

3. **Проверьте Network:**
   - Вкладка "Network" в DevTools
   - Фильтр: "Fetch/XHR"
   - Проверьте запросы к `/api/llm/status` и `/api/llm/pipeline/run`
   - Смотрите статус ответа (200, 400, 500)
   - Смотрите Response body для ошибок

### Проверка API напрямую

```bash
# Проверка статуса
curl http://localhost:8001/api/llm/status

# Запуск pipeline
curl -X POST http://localhost:8001/api/llm/pipeline/run \
  -H "Content-Type: application/json" \
  -d '{
    "date_from": "2025-11-16",
    "date_to": "2025-11-23",
    "run_l4": true,
    "async_mode": false
  }'
```

### Проверка логов бэкенда

```bash
# Если запущен через uvicorn
# Логи должны показывать запросы и ошибки

# Или проверьте логи в терминале где запущен сервер
```

## 🐛 Типичные проблемы

### 1. Кнопка все еще disabled

**Проверьте:**
- Откройте консоль браузера
- Проверьте что `status` загружен: `console.log(status)`
- Проверьте что API `/api/llm/status` возвращает данные

**Решение:**
- Проверьте что бэкенд запущен на `http://localhost:8001`
- Проверьте CORS настройки
- Проверьте переменную `NEXT_PUBLIC_API_URL` в `.env`

### 2. Ошибка при запуске

**Проверьте в консоли:**
- Какая ошибка показывается?
- Какой HTTP статус (400, 500)?
- Что в Response body?

**Типичные ошибки:**
- `column raw_events.occurred_at does not exist` - исправлено, используем `normalized_events`
- `No normalized events to process` - нет данных в БД за указанный период
- `Failed to fetch` - проблема с подключением к API

### 3. API ключи не настроены

**Проверьте:**
```bash
# В .env файле должны быть:
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
PERPLEXITY_API_KEY=pplx-...
```

**Статус будет:**
- `"ready"` - если все ключи настроены
- `"partial"` - если часть ключей настроена (можно запускать)
- Кнопка теперь работает даже при `"partial"`

## 📝 Измененные файлы

- `frontend/app/(dashboard)/dashboard/analytics/llm-pipeline/page.tsx`
  - Изменено условие disabled кнопки
  - Добавлены console.log для отладки
  - Улучшена обработка ошибок
  - Добавлена визуальная подсказка

- `core/api/routes/llm_pipeline.py` (исправлено ранее)
  - Исправлен запрос: используем `normalized_events` вместо `raw_events`

## ✅ Результат

Теперь:
- ✅ Кнопка активна даже при partial статусе
- ✅ Есть отладочная информация в консоли
- ✅ Улучшена обработка ошибок
- ✅ Пользователь видит почему кнопка может быть disabled

---

**Дата создания:** 23.11.2025 02:30

