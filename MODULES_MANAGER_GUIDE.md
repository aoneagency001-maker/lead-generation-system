# 🎛️ Админ-панель управления модулями

Полноценная админ-панель с drag & drop для управления модулями платформ (OLX, Satu и др.)

## ✨ Возможности

- ✅ **Drag & Drop** - перетаскивание модулей между статусами
- ✅ **Kanban доска** - визуальное управление в стиле проект-менеджера
- ✅ **Health Check** - проверка доступности модулей
- ✅ **Статистика** - количество модулей по статусам
- ✅ **Управление** - создание, обновление, удаление модулей
- ✅ **Real-time** - обновление данных в реальном времени

## 🎯 Статусы модулей

- **Активные** (Active) - модули, которые работают в продакшене
- **Тестируются** (Testing) - модули на стадии тестирования
- **Неактивные** (Inactive) - временно отключенные модули
- **Архив** (Archived) - устаревшие или удаленные модули

## 🚀 Быстрый старт

### 1️⃣ Применить SQL схему

Откройте Supabase Dashboard → SQL Editor и выполните:

```sql
-- Применить схему для модулей
-- Файл: core/database/schema_modules.sql
```

Скопируйте содержимое файла [core/database/schema_modules.sql](core/database/schema_modules.sql) и выполните в SQL Editor.

Это создаст:
- ✅ Таблицу `platform_modules`
- ✅ Индексы для быстрого поиска
- ✅ Триггеры для автообновления
- ✅ Тестовые данные (OLX и Satu модули)

### 2️⃣ Запустить Backend API

```bash
# Убедитесь, что в .env настроены:
# - SUPABASE_URL
# - SUPABASE_KEY

# Запустить Core API (порт 8000)
cd /Users/vbut/lead-generation-system
source venv/bin/activate
python -m uvicorn core.api.main:app --reload --host 0.0.0.0 --port 8000
```

### 3️⃣ Запустить Frontend

```bash
# В новом терминале
cd frontend
npm run dev

# Откроется на http://localhost:3000
```

### 4️⃣ Открыть админ-панель

Перейдите по адресу:

```
http://localhost:3000/dashboard/modules-manager
```

## 📖 Как использовать

### Перемещение модулей

1. **Зажмите** иконку ☰ (GripVertical) на карточке модуля
2. **Перетащите** модуль в нужную колонку
3. **Отпустите** - модуль автоматически сохранится с новым статусом

### Проверка здоровья

- **Одного модуля**: кнопка "Health Check" на карточке
- **Всех модулей**: кнопка "Проверить все" в header

### Удаление модуля

1. Переместите модуль в колонку "Архив"
2. Нажмите кнопку корзины 🗑️ на карточке
3. Подтвердите удаление

## 🔧 API Endpoints

Все endpoints доступны на `http://localhost:8000/api/modules`:

### GET

- `GET /api/modules/` - Список всех модулей
- `GET /api/modules/{id}` - Получить модуль
- `GET /api/modules/{id}/health` - Проверить здоровье

### POST

- `POST /api/modules/` - Создать модуль
- `POST /api/modules/batch/reorder` - Массовое изменение порядка
- `POST /api/modules/batch/health-check` - Проверить все модули

### PATCH

- `PATCH /api/modules/{id}/status` - Обновить статус
- `PATCH /api/modules/{id}/order` - Обновить порядок
- `PATCH /api/modules/{id}` - Обновить модуль

### DELETE

- `DELETE /api/modules/{id}` - Удалить модуль

## 📊 Swagger Documentation

После запуска backend API, откройте:

```
http://localhost:8000/docs
```

Там вы найдете полную интерактивную документацию API.

## 🎨 UI Компоненты

Созданы компоненты:

- `ModuleCard` - карточка модуля с drag & drop
- `ModuleColumn` - колонка со статусом (droppable зона)
- `ModulesManagerPage` - главная страница админ-панели

### Стек технологий

- **@dnd-kit** - Drag & Drop библиотека
- **shadcn/ui** - UI компоненты (Card, Badge, Button)
- **Tailwind CSS** - Стилизация
- **Next.js 15** - React фреймворк

## 🔍 Структура данных модуля

```typescript
{
  id: string;
  name: string;                    // "OLX Module"
  platform: string;                // "olx", "satu", "kaspi"
  status: ModuleStatus;            // "active", "testing", "inactive", "archived"
  description: string;             // Описание модуля
  version: string;                 // "1.0.0"
  api_url: string;                 // "http://localhost:8001"
  health_endpoint: string;         // "/health"
  features: string[];              // ["OAuth 2.0", "Парсинг", ...]
  config: object;                  // Конфигурация модуля
  last_health_check: datetime;     // Последняя проверка
  is_healthy: boolean;             // Статус здоровья
  order: number;                   // Порядок отображения
}
```

## 🐛 Troubleshooting

### Модули не загружаются

1. Проверьте, что backend API запущен на порту 8000
2. Проверьте, что SQL схема применена в Supabase
3. Откройте консоль браузера (F12) и проверьте ошибки

### Drag & Drop не работает

1. Убедитесь, что библиотека @dnd-kit установлена:
   ```bash
   cd frontend
   npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities --legacy-peer-deps
   ```

### Health Check не работает

1. Убедитесь, что модули (OLX, Satu) запущены на своих портах
2. Проверьте `api_url` в базе данных - должен совпадать с реальным URL

### Backend API ошибки

1. Проверьте логи в консоли где запущен `uvicorn`
2. Проверьте Supabase credentials в `.env`
3. Убедитесь, что таблица `platform_modules` создана

## 📝 Примеры использования

### Добавить новый модуль через API

```python
import requests

module_data = {
    "name": "Kaspi Module",
    "platform": "kaspi",
    "status": "testing",
    "description": "Модуль для работы с Kaspi.kz",
    "version": "1.0.0",
    "api_url": "http://localhost:8003",
    "health_endpoint": "/health",
    "features": ["API Integration", "Payment Processing"],
    "config": {},
    "order": 2
}

response = requests.post(
    "http://localhost:8000/api/modules/",
    json=module_data
)
print(response.json())
```

### Проверить здоровье всех модулей

```bash
curl -X POST http://localhost:8000/api/modules/batch/health-check
```

## 🎉 Готово!

Теперь у вас есть полноценная админ-панель для управления модулями!

Перетаскивайте модули, проверяйте их состояние и управляйте статусами в реальном времени.

---

**Вопросы?** Проверьте документацию в `/docs/` или откройте issue.
