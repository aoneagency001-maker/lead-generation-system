# 🔄 Обновление .env для Telegram Bot

## ✅ Что изменилось

Код теперь использует **новые имена переменных** для лучшей организации множественных ботов:

- `TELEGRAM_MONITOR_BOT_TOKEN` - токен бота для мониторинга
- `TELEGRAM_MONITOR_CHAT_ID` - chat ID для уведомлений мониторинга

## 🔧 Что нужно сделать

### Вариант 1: Добавить новые переменные (рекомендуется)

Добавь в `.env` файл:

```bash
# ============================================
# TELEGRAM BOTS CONFIGURATION
# ============================================

# Bot для мониторинга и ошибок (Monitoring Bot)
# ⚠️ ВАЖНО: НЕ КОММИТЬТЕ РЕАЛЬНЫЕ ТОКЕНЫ В GIT!
# Используйте GitHub Secrets или локальный .env файл
TELEGRAM_MONITOR_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_MONITOR_CHAT_ID=your_chat_id_here

# Старые переменные (можно оставить для обратной совместимости)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_NOTIFICATION_CHAT_ID=your_chat_id_here
TELEGRAM_SALES_CHAT_ID=your_chat_id_here
```

### Вариант 2: Оставить как есть (работает автоматически)

**Код поддерживает обратную совместимость!** 

Если новые переменные не найдены, код автоматически использует старые:
- `TELEGRAM_BOT_TOKEN` → используется если нет `TELEGRAM_MONITOR_BOT_TOKEN`
- `TELEGRAM_NOTIFICATION_CHAT_ID` → используется если нет `TELEGRAM_MONITOR_CHAT_ID`

**Текущий .env уже работает!** Ничего менять не обязательно.

## 🎯 Зачем новые имена?

Когда у тебя будет несколько ботов:
- `TELEGRAM_MONITOR_BOT_TOKEN` - бот для мониторинга/ошибок
- `TELEGRAM_LEADS_BOT_TOKEN` - бот для уведомлений о лидах
- `TELEGRAM_SALES_BOT_TOKEN` - бот для продаж

Так будет понятно какой бот для чего!

## ✅ Проверка

После обновления проверь:

```bash
# Запусти бота
python -m shared.telegram_bot

# Или протестируй
python scripts/test_telegram_bot.py
```

Если всё работает - готово! 🎉


